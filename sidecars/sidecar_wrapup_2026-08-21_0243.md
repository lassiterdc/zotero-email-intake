---
prompt_doc_type: sidecar_scratch
sidecar_kind: wrapup_handoff
main_scratch: /home/dcl3nd/dev/agentic-workspace/library/_scratch/workspaces/zotero-email-intake_08-15_1300_zotero-email-intake-plugin-pwi.md
last_sync_hashes:
  '### Phase Audit History': 784c9110d7d1bad1dc793bf1c20f544d956f3ad506021cb23c58c4da15dcbabe
  '# Follow-up Ideas': 4f8f725ee51395fd5b99f870651a757bd8110acd14ced8ecc7f87f353cb4256a
harness: claude-code
plan_name: zotero-email-intake-plugin
plan_completion_commit: 292f4e209a0d5c6408bf664d6a412f9fbac6cbc9
worktree_path: /home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi
created: '2026-08-21T02:43:26'
custom_instructions:
  - 'MANDATORY FULL READ — unconditional, and BEFORE any other action in this resume rather than only before a specialist action: the # SESSION OPERATING MANDATE section (the first H1) of the main session scratch. Treat it as item 0 of the Mandatory reads list and give it its own wc -l receipt and read-coverage self-assertion in the Part 1 report. It is the ONE source of truth for: resume budgets (section 2, UNLIMITED); the specialist implementation protocol and VMS AUTHORSHIP (section 3 — the SPECIALIST writes every VMS and the main agent never does; the finalized spec in the specialist scratch is the single source of truth and every other surface points at it rather than restating it; a specialist halt is a RESUME-and-workshop trigger, which OVERRIDES the delegated-implementation contract routing a DIFF-QC PLAN-GAP to /handle-friction); the four user halt conditions (section 4); and the clean-tree precondition (section 5). Do not re-derive any of it and do not act on any resume-count figure found elsewhere in the scratch.'
  - 'Per-Phase Preflight step 4 names a master decisions log that this master does NOT have — it carries only ## Phase Status and ## Phase Records. Run plan-sync against ## Phase Records instead. The Phase 3 closeout record there is the one Phase 3b must read; the Phase 2 forward note this instruction previously pointed at is spent, its three CI items having been closed in Phase 3.'
  - 'check_plan_anchor_freshness reports roughly 65 target-not-found FAILs on the zotero-specialist scratch and this is NOT a regression: 3 hardening.md moved to implemented/ at 6846dccad6, so those applied-fence specs are permanently ungradeable. Do not try to repair them and do not read them as a signal about new work. Judge a NEW spec by the three-number signature instead — targets-resolved and old-text-checks RISE while findings and FAILs hold constant.'
  - 'Phase-doc priority and loe fields are corpus-exempt and must NOT be recomputed. generate_planning_tables.py line 256 skips planning_doc_type: phase whenever the doc sits in a multi-phase directory, so recompute_priority never reaches a phase doc, and _structural.py exempts all six PRIORITY_FIELDS deliberately because phase docs inherit priority from the master sibling. The value priority: 1.95 on 3b is stale-by-design against loe: High, and writing 1.65 would reinstate exactly the drift the exemption prevents.'
smoke_test: false
last_sync_hashes_normalized:
  '### Phase Audit History': 909c476091c8047bbc02ca4c3c74f3bf04a90ba0644dee04fd83b85e571923e9
  '# Follow-up Ideas': 2eab4eeb8fdca3d69088358fac77396bdbe33f2d5af0ddb84d60e5b1fc047644
---
- [ ] in progress

## Harness Discipline — Claude Code

- Treat the first post-compaction message as a rehydration trigger, not a
  fresh-session invocation. Do NOT run `/proceed-with-implementation`
  preflight. Do NOT re-enter the phase loop. Plan Completion (phase-doc
  moves to `implemented/`, master to `completed/`, planning-table
  regeneration, commit) already ran in the pre-compaction session.
- Your task is `/wrapup-session` in full, per `$AGENTIC_WORKSPACE/library/prompts/instructions/skills/wrapup-session/wrapup session SKILL.md`.
- Do NOT read the main session scratch doc until after wrapup-session Step 6
  (`--inject-into-scratch`) has run. Immediately after that injection — and
  before populating any wrapup sections — read the just-injected wrapup
  section AND the `# scratch doc communication protocols` section of main
  scratch in full. Only then proceed to Step 7 of the skill.
- Sync integrity is verified via
  `scripts/generate/write_compaction_sidecar.py --mode verify`. Do not
  compute hashes yourself.

## Session Custom Instructions

These instructions are specific to this session and were supplied by the user (or agent-recommended and user-approved) at a prior compaction gate. They ride along with every sidecar until session close and must be honored in addition to — not instead of — the standard mandatory reads and the composed atomic content below.

- MANDATORY FULL READ — unconditional, and BEFORE any other action in this resume rather than only before a specialist action: the # SESSION OPERATING MANDATE section (the first H1) of the main session scratch. Treat it as item 0 of the Mandatory reads list and give it its own wc -l receipt and read-coverage self-assertion in the Part 1 report. It is the ONE source of truth for: resume budgets (section 2, UNLIMITED); the specialist implementation protocol and VMS AUTHORSHIP (section 3 — the SPECIALIST writes every VMS and the main agent never does; the finalized spec in the specialist scratch is the single source of truth and every other surface points at it rather than restating it; a specialist halt is a RESUME-and-workshop trigger, which OVERRIDES the delegated-implementation contract routing a DIFF-QC PLAN-GAP to /handle-friction); the four user halt conditions (section 4); and the clean-tree precondition (section 5). Do not re-derive any of it and do not act on any resume-count figure found elsewhere in the scratch.
- Per-Phase Preflight step 4 names a master decisions log that this master does NOT have — it carries only ## Phase Status and ## Phase Records. Run plan-sync against ## Phase Records instead. The Phase 3 closeout record there is the one Phase 3b must read; the Phase 2 forward note this instruction previously pointed at is spent, its three CI items having been closed in Phase 3.
- check_plan_anchor_freshness reports roughly 65 target-not-found FAILs on the zotero-specialist scratch and this is NOT a regression: 3 hardening.md moved to implemented/ at 6846dccad6, so those applied-fence specs are permanently ungradeable. Do not try to repair them and do not read them as a signal about new work. Judge a NEW spec by the three-number signature instead — targets-resolved and old-text-checks RISE while findings and FAILs hold constant.
- Phase-doc priority and loe fields are corpus-exempt and must NOT be recomputed. generate_planning_tables.py line 256 skips planning_doc_type: phase whenever the doc sits in a multi-phase directory, so recompute_priority never reaches a phase doc, and _structural.py exempts all six PRIORITY_FIELDS deliberately because phase docs inherit priority from the master sibling. The value priority: 1.95 on 3b is stale-by-design against loe: High, and writing 1.65 would reinstate exactly the drift the exemption prevents.

## Wrapup Entry Point

- Kind: wrapup_handoff
- Plan name: zotero-email-intake-plugin
- Plan completion commit: `292f4e209a0d5c6408bf664d6a412f9fbac6cbc9`
- Worktree: `/home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi`
- Worktree branch: `worktree-08-15_1300_zotero-email-intake-plugin-pwi`
- Task: `/wrapup-session` in full

## Post-Compaction Wrapup — Preamble

You are resuming a multiphase plan implementation after an in-session /compact
that was fired at the end of the plan — after the final phase committed and
after Plan Completion (plan closeout protocol) ran in the pre-compaction
session. Your single task is `/wrapup-session` in full. Do NOT re-enter the
phase loop. Do NOT re-run Plan Completion. It is already done.

The first message you receive after compaction looks structurally like a fresh
session invocation, and RLHF training creates a pull to treat it as one —
running preflight, re-reading the full session scratch doc, re-dispatching
specialist reviews. Recognize this pull. Your actual task is to rehydrate from
the wrapup-handoff sidecar, verify sync integrity via the script, run the
First Action GO gate, and then invoke `/wrapup-session` directly.

### Mandatory reads (in order, each in full)

1. Wrapup-handoff sidecar:
   `/home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi/sidecars/sidecar_wrapup_2026-08-21_0243.md`

2. Wrapup-session skill body (the skill you are about to execute):
   [wrapup session SKILL](../../instructions/skills/wrapup-session/wrapup%20session%20SKILL.md) #inject_path

%% inject-path-start: wrapup-session-skill %%
  - `$AGENTIC_WORKSPACE/library/prompts/instructions/skills/wrapup-session/wrapup session SKILL.md`
    - Session wrap-up checklist — audits for uncommitted changes, outstanding todos, unresolved decisions, and next steps before exiting
%% inject-path-end: wrapup-session-skill %%

### Wrapup entry point

- **Plan name**: `zotero-email-intake-plugin`
- **Plan completion commit**: `292f4e209a0d5c6408bf664d6a412f9fbac6cbc9`
- **Worktree path**: `/home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi`
- **Worktree branch**: `worktree-08-15_1300_zotero-email-intake-plugin-pwi`
- **Task**: run `/wrapup-session` in full, per the skill body above.

Plan Completion already moved all phase docs to `implemented/` and the master
plan subdirectory to `completed/`. Regeneration of planning tables and the
plan closeout commit are in git history at `292f4e209a0d5c6408bf664d6a412f9fbac6cbc9`. If you
find yourself tempted to re-run `$AGENTIC_WORKSPACE/scripts/generate/complete_plan.py` or touch the planning
tree, stop — that work is done.

Do NOT read the main session scratch doc yet. Main scratch is intentionally
out of scope for the First Action gate. You will read it at wrapup-session
Step 6 — see the "Step 6 injection-then-reload contract" below.

### Step 6 injection-then-reload contract (read this carefully)

When `/wrapup-session` reaches Step 6 (Write Report to Session Scratch Doc),
the skill runs `create_from_template --compose wrapup-session
--inject-into-scratch /home/dcl3nd/dev/agentic-workspace/library/_scratch/workspaces/zotero-email-intake_08-15_1300_zotero-email-intake-plugin-pwi.md` to append the wrapup template
body into main scratch under a new `# Wrapup Session` heading. Once that
injection command returns success and **before you begin populating any
section or proceeding to Step 7**:

1. **Read the just-injected wrapup section in main scratch in full.** This is
   the wrapup-session template that Steps 0-5 findings will be written into.
2. **Read the `# scratch doc communication protocols` section of main scratch
   in full.** This loads the agent reply conventions (callout color semantics,
   agreement-checkbox format, datetime stamps, quoted-user-comment format) and
   the user comment resolution protocol into your context. These are the
   conventions for every user-facing callout you will write during wrapup
   Step 7 (follow-up review) and Step 8 (Verdict).

Only after BOTH reads are complete, continue with wrapup-session by populating
the injected sections from your Step 0-5 findings and proceeding to Step 7.
This reload step is not optional — the communication protocols are the
delivery contract between you and the user for the remainder of the session.

### Worktree context

- **Worktree path**: `/home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi`
- **Worktree branch**: `worktree-08-15_1300_zotero-email-intake-plugin-pwi`
- **Main scratch path**: `/home/dcl3nd/dev/agentic-workspace/library/_scratch/workspaces/zotero-email-intake_08-15_1300_zotero-email-intake-plugin-pwi.md`

All wrapup-session writes go to the worktree (or to main scratch via the
Step 6 injection). Worktree closeout runs as part of `/wrapup-session` Step
7b — push the branch, run merge_review, merge to main, run `$AGENTIC_WORKSPACE/setup.sh` from
the main-tree root, commit regenerated files, and remove the worktree.

### Downstream-skill fidelity framing

Any skill invoked from within this wrapup session (for example, `/review-followup-ideas`, `/plan-implementation`, or `/capture-idea` for follow-up items) runs against approximately fresh context — compaction just freed it. Treat those skills as first-invocation fresh-session runs for fidelity purposes. Do not cite the pre-compact session length, the conversation summary size, or "how much has already happened today" as a reason to compress a skill's gates. If the developer surfaces a concrete context-budget constraint, respond to it; otherwise perform at full fidelity.

## Post-Compaction Wrapup — Halt Triggers

### Halt triggers

1. **Tier 1 wrapup gate**: any of the wrapup-session hard gates fail and
   cannot be remediated within the skill — specifically Step 1 (Unresolved
   User Comments) cannot be cleared, Step 7 (Follow-up Idea Review) cannot
   reach terminal disposition on every idea, or the Verdict step reports
   unresolved items. Halt and surface the specific gate to the user.
2. **Worktree closeout failure**: any of the gated steps in worktree closeout
   (clean-tree check, merge_review, merge conflict resolution, `$AGENTIC_WORKSPACE/setup.sh`
   run, worktree removal) fails. Do not attempt destructive recovery — halt
   and surface the failure mode.
3. **Sync-back integrity failure**: `write_compaction_sidecar.py --mode verify`
   exits with code 2 (hash mismatch) or code 3 (worktree dirty). Never
   overwrite main on a mismatch.
4. **Sidecar consistency failure** — see 4-condition checklist below.
5. **Explicit user interrupt**.

#### Halt trigger #4 — sidecar consistency failure

Trigger if ANY of the following four conditions fail when you read the
wrapup-handoff sidecar:

1. The sidecar frontmatter's `sidecar_kind` field is exactly `wrapup_handoff`.
2. The sidecar body contains a `## Wrapup Entry Point` section.
3. The sidecar's `## Worktree Status` section's branch line matches the
   sidecar frontmatter's `worktree_path` field's derivable branch name
   (`worktree-{slug}`).
4. The sidecar's `last_sync_hashes:` frontmatter contains exactly two entries:
   `### Phase Audit History` and `# Follow-up Ideas`.

Write each condition's result explicitly as
`Condition N: PASS|FAIL — [observed vs. expected]` before producing the GO
signal. Any FAIL halts.

### Post-halt response protocol

When any halt trigger fires:

1. **Write the halt state** to the sidecar's `# Follow-up Ideas` section with:
   - The halt trigger number that fired
   - The specific condition or evidence that triggered it
   - The wrapup step at which the halt was detected (if applicable)

2. **Do NOT run `--mode sync-back`** on a wrapup-handoff sidecar. Sync-back
   is a phase-loop operation; the wrapup sidecar is not an in-flight sidecar
   and sync-back will refuse or misbehave. If the halt state needs to reach
   main, write it to MAIN's `# Follow-up Ideas`, not to the sidecar's. No
   scheduled step flushes a wrapup-handoff sidecar, and `/wrapup-session`
   Step 7 reads MAIN — content written to the sidecar at this point is
   destroyed with the worktree at Step 7b. The sidecar does not freeze main's
   writable sections (the freeze set is the sync-back-bearing kinds only), so
   the main write is permitted. It does move main away from the sidecar's
   recorded `last_sync_hashes`, so a LATER `--mode verify` in this same
   worktree will report drift on `# Follow-up Ideas`. That is expected after
   a wrapup handoff and is not halt trigger #3: resolve it with
   `write_compaction_sidecar.py --mode rebaseline-from-main`, which backs main
   up reversibly, re-records the hashes, and asserts verify passes.

3. **Wait for user direction**. Do not attempt to diagnose, work around, or
   retry the halt condition on your own. Produce a single-message halt
   report: "Halted: trigger #N — {one-line description}. Awaiting direction."

4. **Route on user direction**: either the user resolves the blocker and
   authorizes continuation with a specific next step, or the user instructs
   a fallback path (manual worktree closeout, deferred wrapup, etc.).

## Post-Compaction Wrapup — First Action

### Part 1 — Read-coverage report (presumption-STOP gate)

**Presumption**: you have not read any files. The default state is "reads not
yet performed." Proceeding to Part 2 requires affirmative evidence of each
mandatory read. RLHF completion-bias creates a pull to produce a
plausible-looking table without having actually done the reads — recognize
this pull and resist it.

For each mandatory read (the wrapup sidecar and the wrapup-session skill
body), run `wc -l {absolute-path}` in a Bash tool call and paste the literal
stdout output into the assertion block. Prose descriptions of "I read this
file" are not acceptable — only `wc -l` stdout counts.

After each `wc -l` receipt, write a one-line self-assertion of the form
`Read coverage: all N lines read, understanding consistent with length.`
substituting the observed N. Both the `wc -l` output and the self-assertion
are required. If you cannot truthfully write the self-assertion — because
you did not actually read the file in full, or because your understanding is
inconsistent with the observed length — HALT. Do not fabricate the
self-assertion to satisfy the gate.

Main scratch is intentionally NOT in this read-coverage list. It is read
during wrapup-session Step 6 per the injection-then-reload contract in the
preamble — do not read main scratch here.

Format:

```
$ wc -l /home/.../sidecars/sidecar_wrapup_2026-04-15_1500.md
87 /home/.../sidecars/sidecar_wrapup_2026-04-15_1500.md
Read coverage: all 87 lines read, understanding consistent with length.
$ wc -l /home/.../library/prompts/instructions/skills/wrapup-session/wrapup session SKILL.md
410 /home/.../library/prompts/instructions/skills/wrapup-session/wrapup session SKILL.md
Read coverage: all 410 lines read, understanding consistent with length.
```

If any `wc -l` count does not match your understanding of the file's length —
or if you cannot produce `wc -l` output for a file you claim to have read —
or if you cannot truthfully write the per-file self-assertion — HALT. That
is the presumption-STOP gate firing. Do not fabricate counts or
self-assertions; run the tool and read the files.

### Part 2 — Wrapup-entry assertion

Read the sidecar frontmatter and the sidecar's `## Wrapup Entry Point`
section. Write:

`Wrapup-entry assertion: Sidecar: {sidecar_filename}. sidecar_kind: wrapup_handoff. Plan name: zotero-email-intake-plugin. Plan completion commit: 292f4e209a0d5c6408bf664d6a412f9fbac6cbc9. Wrapup entry block present in body: YES|NO.`

If any of these do not match the sidecar's actual state, or if the body does
not contain `## Wrapup Entry Point`, HALT per the post-halt response protocol
and surface the specific inconsistency.

### Part 3 — Sync integrity assertion (via script, NOT by reading main)

Do NOT read the main scratch doc. Do NOT compute hashes yourself. Invoke:

    scripts/generate/write_compaction_sidecar.py --mode verify \
      --main-scratch /home/dcl3nd/dev/agentic-workspace/library/_scratch/workspaces/zotero-email-intake_08-15_1300_zotero-email-intake-plugin-pwi.md \
      --worktree /home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi

Assert the exit code:

- Exit 0: write `Sync integrity (from --mode verify exit code): OK. Worktree: clean.`
- Exit 2: script detected a hash mismatch. HALT per post-halt response
  protocol. Do NOT attempt recovery.
- Exit 3: script detected uncommitted changes in worktree. HALT — the
  pre-compaction session did not finish committing cleanly.

### Conjunctive GO gate (required written assertion before /wrapup-session)

Produce exactly the following assertion text verbatim, substituting GO or
STOP for each Part:

```
Part 1: GO|STOP
Part 2: GO|STOP
Part 3: GO|STOP
Conjunction: (Part 1 = GO) AND (Part 2 = GO) AND (Part 3 = GO) = GO|STOP
Proceeding to /wrapup-session: YES|NO
```

The `Proceeding to /wrapup-session: YES` line is the single gate event. Any
STOP in any Part means NO. Writing the conjunction is required — it is the
forcing function against RLHF task-completion pressure that would otherwise
push the agent to transition silently.

Then invoke `/wrapup-session` directly and follow it in full. Do not
summarize what you learned — the sidecar is the record. Do not ask the user
for confirmation before starting the skill — the GO gate IS the
confirmation.

### Phase Audit History



| Phase | Phase-Local Diff Files | Audit-on-Diff Violations | Resolution |
|---|---|---|---|
| 0 | `manifest.json`, `bootstrap.js`, `src/intake.js` (new), `docs/architecture.md` | N/A — audit-on-diff skipped (cross-repo project PWI; `library/prompts/instructions/` absent from the project worktree, so `scripts.audit`'s universe does not cover these files). Pytest gate UNAVAILABLE for the same structural reason. | Substitute coverage run per the cross-repo guard: the plan's 9 Validation Plan items, all green, plus a live-Zotero DoD pass (7/7). One latent donor defect found by the disable/re-enable DoD item — `shutdown()` unregistered the notifier observer without clearing `itemNotifierID`, so re-enable silently skipped re-registration — fixed in-phase. Four plan defects found and amended (Corrections 2 and 3, Validation item 8, DoD item 5). |


| 1 | `src/intake.js`, `src/message.js` (new), `src/probe.sys.mjs` (new), `locale/en-US/emailintake.ftl` (new), `bootstrap.js`, `docs/architecture.md` | N/A — audit-on-diff skipped for the same structural reason recorded at Phase 0 (cross-repo project PWI; `library/prompts/instructions/` absent from the project worktree). Pytest gate UNAVAILABLE identically. | Substitute coverage per the cross-repo guard, all green: Validation Plan items 1–7 re-run by the main agent independently of the specialist that reported them, plus `R14` purity (0 `Zotero` tokens in `src/message.js`) and zero top-level `const`/`let`/`class` in either loader-evaluated file. Live-Zotero DoD 12/12 including the two-packaging ESM spike. Diff is exactly the six declared `affected_files`. TWO deviations, both surfaced rather than improvised and both rolled into the plan before implementation continued: `_shuttingDown` had no reachable setter (the loader has no `shutdown` hook and its only reachable callback is shared with `onMainWindowUnload`), fixed by a fourth `bootstrap.js` edit; and `mapToPayload` was told to read a pref from a file forbidden to name `Zotero.`, resolved by the user under §4 condition 1 as a parameter. SPIKE FAILED UNDER BOTH PACKAGINGS (`Error: System modules must be loaded from a trusted scheme`), which by the plan's own rule settles Phase 2's module strategy as `module.exports`. One environmental prerequisite found that no code change satisfies — ZotMoov's `allowed_fileext` omits `eml` by default — now recorded in master assumption A2 and in `docs/architecture.md`. Stop gate initially FAILED Condition 3 on that prerequisite and cleared after A2 was amended. |

| 2 | `src/message.js`, `src/cfb.js` (new), `package.json` (new), `.gitignore`, `docs/architecture.md`, `test/parse.test.js` (new), `test/pairing.test.js` (new), `test/local-corpus.test.js` (new), twenty `.eml`/`.expected.json` fixtures (new), `scripts/make-msg-fixture.js` (new) — 29 paths | N/A — audit-on-diff skipped for the same structural reason recorded at Phases 0 and 1 (cross-repo project PWI; `library/prompts/instructions/` absent from the project worktree). Pytest gate UNAVAILABLE identically. | Substitute coverage per the cross-repo guard, all green: Validation items 1–6, five of six replayed independently by the main agent rather than accepted from the specialist, including a fresh re-demonstration of item 2's negative control at QAQC time (`pass 11 / fail 2`, restored to `14 / 13 / 0 / skip 1`). Diff-QC PASS with the bijection exact at 29/29 against the fence as amended at five sites. DoD 8/8 resolved — 7 met, the stretch-descope counterpart correctly N/A because the stretch LANDED. Implemented by `zotero-specialist` under §3 across an initial dispatch plus seven resume rounds, with the specialist authoring every VMS after workshopping to concurrence; the main agent authored none, per the §3 amendment landed this session. TWO plan defects corrected at preflight (a `mapToPayload` signature that would have reversed a §4-condition-1 ruling, and an unwired `detect`). TWO implementation deviations, both halts rather than improvisations: the plan's `node --test test/` invocation is FALSE on Node 24 (`MODULE_NOT_FOUND`, reproduced independently), resolved to the quoted-glob form after measuring all three candidates and finding that Node's `kDefaultPattern` counts a non-test `.js` under `test/` as a PASSING test; and the `.gitignore` write required the developer approval its harness gate names. The `engines` floor was falsified as a side effect (glob positionals need Node 21; 20 and 21 are both EOL) and set to `>=22` by developer ruling. The stretch's real-corpus acceptance bar exists because of a measurement: a deliberately crippled reader PASSES the plan's own cross-container check while extracting from ZERO of eighteen real containers, failing with a clean `null` indistinguishable from correct declining. Two residuals recorded rather than closed — the anti-vacuity hole no invocation form closes (routed to Phase 3's CI suite) and the coverage oracle's embedded-message false positive. One product finding surfaced for the developer: 39% of real `.msg` files carry no `PR_TRANSPORT_MESSAGE_HEADERS`. Master V5 widened from `.eml` to `.eml|.msg` and re-scoped from path to provenance, because this phase introduces real `.msg` files and V5 is R19's only mechanical enforcement. |

| 3 | `bootstrap.js`, `docs/architecture.md`, `locale/en-US/emailintake.ftl`, `package.json`, `src/intake.js`, `src/message.js`, `.github/workflows/ci.yml` (new), `prefs.js` (new), `prefs.xhtml` (new), `scripts/ci/invariants.sh` (new), `scripts/ci/run-tests.sh` (new), `test/redos.test.js` (new) — 12 paths | N/A — audit-on-diff skipped for the same structural reason recorded at Phases 0, 1 and 2 (cross-repo project PWI; `library/prompts/instructions/` absent from the project worktree, so `scripts.audit`'s universe does not cover these files). Pytest gate UNAVAILABLE identically. | Substitute coverage per the cross-repo guard, all green: Validation items 1, 2 and 3 run this session; 3b and 3c carried as RECORDED from the pre-rewind conversation; 4 and 5 are the developer's live-Zotero runs. **DoD 13/13.** The principal QAQC result is that DoD row 2 — the negative control, this phase's centerpiece — had never been run, and was run here: eleven seeded violations produced exactly eleven findings, one per rule id, exit 1, so no rule is inert. A second run corrected a seed-PLACEMENT defect in the first: Validation item 2 requires the R04 seed land in `test/` rather than `src/` because that is the rule's actual risk surface, and the first run seeded it from `src/`, firing the rule while demonstrating less than the item asks; re-run from `test/seed-r04.js` produced exactly one R04 finding at exit 1. Restoration verified by md5 against pre-seed values, not asserted — tree back to 13 paths, invariants exit 0 silent, suite back to 16/15/0/1. One DoD row recorded as VACUOUSLY satisfied rather than passing: `getContentsAsync(` occurs in `src/` only inside a comment at `src/intake.js:147`, so "every call site passes a third argument" is true of an empty set. Preflight had halted at form 2 over seven gaps, closed by a `/handle-friction` needs-research round in which `zotero-specialist` authored 15 specs across two rounds under §3; that round corrected three main-agent briefing claims (the fresh-profile inertness is a deliberate kill switch; `setDefaultPrefs` auto-loads a plugin-root `prefs.js` so item 4 was ONE defect; the `recipientCap` NaN path is already guarded) and surfaced two findings no gap had named (`pretest` does not fire for `npm run ci`, and `readFields` built its field map on a plain object). Three live-Zotero defects found by the developer and fixed: raw `{$count}`, the summary window never dismissing, and a `.msg` drop showing no window that did not reproduce on the fixed build and whose pre-fix cause is UNEXPLAINED and not guessed at. Developer ruling under §4 condition 2: `closeOnClick: true`, restoring the platform default the donor had overridden. Corpus `--critical-only` backstop exit 0 with 0 findings attributable to Phase 3; one pre-existing `AFFECTED_FILE_OUTSIDE_WORKSPACE` on `4 publish.md` classified rather than actioned, verified pre-existing at `033e0b231b^`. |

| 3b | `src/intake.js`, `docs/architecture.md` (project); `3b duplicate detection and resolution.md` (AW) — 3 paths | N/A — audit-on-diff skipped for the same structural reason recorded at Phases 0, 1, 2 and 3 (cross-repo project PWI; `library/prompts/instructions/` absent from the project worktree, so `scripts.audit`'s universe does not cover these files). Pytest gate UNAVAILABLE identically. | Substitute coverage per the cross-repo guard, all green: `invariants.sh` exit 0 silent (run bare), `npm run ci` 17/16/0/1, Validation item 12 re-verified, and the developer's ten-item live-Zotero walkthrough across THREE runs — all pass. **DoD 14/16 MET**, rows 9 and 16 discharging at the commit itself. The phase's centrepiece result is that DoD row 4 — every menu command appears only on a selection it applies to — FAILED on run 2 and was the phase's sole blocker: `Zotmail: Re-promote / regenerate key` appeared on any parented file attachment including an ordinary PDF, and was not inert when clicked, renaming that PDF on disk from the parent's metadata and overwriting its title. Root cause was a PLAN defect faithfully implemented — the spec said "on a parented attachment" and stopped — and the `onShowing` contract turned out to be stated at THREE altitudes across phase-doc lines 168-182, of which the first fix amended only one. Closed by a `/handle-friction` needs-research round opened as `# Sidequest 0 — phase3b-menu-predicate-and-counter-traps`, in which `zotero-specialist` authored VMS 35-41 across rounds 7-8 under §3 and applied them across rounds 9-10; all seven verified textually by the main agent rather than accepted, including the old-text-removal check that catches an append-instead-of-replace. That round also REFUTED a main-agent premise — that `Promote to E-mail item` on a withheld duplicate is a silent no-op — by tracing `summariseBatch`'s unconditional call and the `itemCount <= 1 && nonPromoted === 0` display guard; the refutation was verified in source and recorded in the plan as VMS 37 so the correct behaviour is not re-litigated as a bug. A §5 clean-tree deadlock blocked VMS 36 (dirty project tree vs a `docs/architecture.md` that must ride the phase commit) and was resolved by developer ruling under §4 condition 1: PARK the dirty files, dispatch, restore, with byte-exactness confirmed by md5 — explicitly not a standing precedent. Two instrument traps that had each cost a full manual re-run were given a durable carrier in Validation item 5 and its DoD row (VMS 38-39): Zotero's Run JavaScript rejects a top-level `return` outside Async mode, and `_searchCount` is zeroed by any plugin reload while the walkthrough's own ordering puts a disable/re-enable before later readings. Standing scope caveat: `src/intake.js` has NO Node harness, so the 17/16/0/1 count was identical before and after VMS 36 and is a regression check only — the sole instrument reaching the new predicate is the developer's manual Test 8(b-ii). |
| 4 | `README.md`, `docs/architecture.md`, `locale/en-US/emailintake.ftl`, `prefs.js`, `src/intake.js`, `update.json` (new), `.github/workflows/release.yml` (new) — project; `4 publish.md` — AW. 8 paths. `manifest.json` correctly UNTOUCHED: the version ruling made its entry a confirmation rather than an edit. | N/A — audit-on-diff skipped for the same structural reason recorded at Phases 0, 1, 2, 3 and 3b (cross-repo project PWI; `library/prompts/instructions/` absent from the project worktree). Pytest gate UNAVAILABLE identically. The `aw` bucket audit over the phase doc ran: `totalDocs=1`, `errors=0`, `warnings=0`. | Substitute coverage is PARTIAL BY DESIGN and that is the phase's defining property: items 1, 2, 3 and 5 all require a published release or a running Zotero, so the DoD is not closable by agent action. Item 4 — the fixture-privacy history check — WAS run and PASSES: exactly 10 `.eml` paths ever added on any branch, all under `test/fixtures/eml/`, every domain RFC 2606 reserved. `invariants.sh` exit 0 bare; `npm run ci` 17/16/0/1 with `src/intake.js` carrying zero automated coverage over 178 of the 242 inserted lines; `label:` 0 and `l10nID` 4 re-verified; `node --check`, JSON and YAML validated because the suite parses none of them. Implemented by `zotero-specialist` under §3 across FOUR halts before the first edit, each surfacing a plan defect rather than an implementation error: the exclude-list build would have shipped `.git/` — the repository's entire history — inside every installed XPI, defeating the phase's own fixture-privacy DoD row in the artifact that row protects, fixed with a fail-closed INCLUDE list on the `R12` reasoning; `update.json` was uncreatable as specified because no XPI exists to hash before the first tag, resolved as a skeleton after a placeholder digest was REJECTED as a false integrity claim on a publicly-fetchable channel; and the ZotMoov prompt's scope was widened from the drop path to the menu path by MECHANISM — ZotMoov's `modifyCallback` applies no filter and re-arms the same debounced automove pass — which no drag-shaped DoD row would ever have caught, so P4-11 adds the missing row. Two anchor defects were caught pre-apply, the sharper one being that an eight-space `const batchMap = new Map();` is a SUBSTRING of a sixteen-space occurrence, so a line-anchored grep clears it while Edit hits two sites. A tree-agreement gap was found by reading the workflow YAML rather than its description: `ref: main` would have built the XPI from the branch while the tag named another commit, invisible to the version guard because a tree divergence changes no version string. |

## Follow-up Ideas


%%
"#" + "followup" tag: When the user tags an inline comment with `#` + `followup`, cut the tagged text from its original location, paste it here as a bullet, and add context sub-bullets if the original text does not stand alone without its surrounding context. The agent judges whether sub-bullets are needed — if the comment makes sense on its own, no sub-bullets are required. Change the tag in the original location to `#followup-moved`.

Specialist findings routed here by /invoke-specialist Step 6 appear under:
  ### Specialist-identified follow-up items
  #### {specialist-name}
These are appended via append_scratch_section.py and are automatically picked up by /review-followup-ideas.

Dual-orthogonality criterion (mandatory). An item belongs here ONLY if it is orthogonal to BOTH (a) the immediate task/skill purpose (the current phase or invocation) that surfaced it, AND (b) the overall session purpose. Anything that is NOT orthogonal — i.e., in-scope work bearing on the immediate task or the session's objectives and target outcomes — does NOT go here: act on it in-band or route it to the relevant primary output. If the immediate task purpose and the session purpose coincide, apply the single combined test. RLHF training pulls toward dumping every surfaced thought into a follow-up list; resist it — an item that shares a file or design surface with the immediate task or session purpose is in-scope work, not a follow-up.

Precedence (absolute): "orthogonal to the current phase or invocation" NEVER overrides "in-scope for the session's objectives and target outcomes." An item whose non-resolution would ship a silently-broken or misleading PUBLIC artifact of the session's target outcome — a published package/wheel and its dependency metadata, the docs site, the README, a CITATION/DOI/release record, or a CI check whose green is misleading — is in-scope for the session purpose regardless of how orthogonal it is to the phase in hand, so it must NOT be routed here; it belongs in the primary work/output surface as release-blocking. Routing such an item to follow-ups is a category error, not a scoping judgment.
%%


### Session 7e75b477-132d-4902-91d0-577981b92316

- [friction-followup] No plan-review rubric exists for zotero-specialist; `_resolve_plan_review_key()` has no fallback, so a plan-review dispatch for it would build an unresolvable registry key. 17 rubrics exist; zotero-specialist is absent. User accepts the weakness for now and removed it from this plan's `pre_implementation_reviews` — capture is for the durable gap, not this plan. Sibling ideas exist for athletic-training and technical-writing specialists. — remedy-paths: library/prompts/templates/template_registry.yaml, library/prompts/templates/atomic/specialist/; engaged-set: library/prompts/instructions/skills/proceed-with-implementation/pwi common prefix.md, library/_scratch/.sessions/7e75b477-132d-4902-91d0-577981b92316.json, session scratch and satellites, library/prompts/templates/template_registry.yaml, library/prompts/templates/atomic/pwi preflight satellite body.md, scripts/generate/create_from_template.py, scripts/generate/compose_satellite.py

- [friction-followup] No setup path installs git hooks in a sibling project repo — `scripts/setup/repair.sh`'s install-and-parity ladder is scoped to $WORKSPACE and sits outside any per-workspace loop. `zotero-email-intake` has neither installed hooks nor a .pre-commit-config.yaml, so nothing fires at git commit there. — remedy-paths: scripts/setup/repair.sh, managed_workspaces.yaml; engaged-set: same as the row above

- [friction-followup] Corpus-wide frontmatter debt: `frontmatter_gate --human` exits 1 with 411 errors / 2499 warnings; the sampled errors are idea docs missing `affected_files` plus two hhemt SKILL.md files missing `prompt_doc_type`. None is on a file this plan touches. — remedy-paths: library/docs/planning/projects/agentic-workspace/ideas/*.md, library/prompts/workspaces/projects/hhemt/skills/*/SKILL.md; engaged-set: same as the rows above


- [friction-followup] The `software-engineering` tool workspace has NO architecture doc — `check_architecture_staleness software-engineering` exits 2 with NO_ARCHITECTURE_DOC. Surfaced at the /invoke-specialist pre-invocation gate for the zotero-email-intake plan review; user chose to proceed and route here rather than spinup mid-PWI. Remedy: `/invoke-specialist software-engineering-specialist --purpose spinup-architecture`. remedy-paths: library/prompts/workspaces/tools/software-engineering/ (architecture doc, not yet created); engaged-set: library/_scratch/specialist_calls/2026-08-15_1300_software-engineering-specialist_plan-review.md, the zotero-email-intake-plugin plan family, library/prompts/instructions/skills/proceed-with-implementation/pwi common prefix.md

- [friction-followup] The roll-in protocol prescribes a commit-message form that its own commit hook rejects. `specialist plan review roll in.md` section 5 mandates `docs(planning-infrastructure): roll in {specialist-name} plan-review specs ({phase})`; substituting `software-engineering-specialist` and `PWI preflight` yields 73 characters after the colon-space against `check-commit-message.py`'s 72-character limit, so the mandated form is unsatisfiable for the longest-named specialist and nothing connects the two rules. Measured 2026-08-15 by running the hook against the composed summary. Remedy candidates: shorten the prescribed form, or drop the parenthetical phase from the summary and carry it in the body. — remedy-paths: library/prompts/instructions/protocols/specialist plan review roll in.md; engaged-set: scripts/audit/checks/_naming.py, scripts/audit/checks/_planning_hygiene.py, tests/audit/test_frontmatter_path_validity.py, tests/audit/test_rules_planning_hygiene.py, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3 hardening.md

- [friction-followup] Four audit tests fail at agentic-workspace main independent of any branch work, and nothing currently surfaces them. Measured 2026-08-15: the full `tests/audit/` suite returned 4 failed / 720 passed / 4 skipped, and the identical four node-ids fail at control `b89b05fa09` (the merge-base), giving branch-introduced 0 and inherited 4. The four are `test_config.py::TestValidValues::test_harness_scope_values`, `test_decorator_registry_matches_baseline.py::test_decorator_registry_matches_baseline`, `test_scope_bucket_membership.py::test_bucket_membership_is_locked`, and `test_section9_rule_soak.py::test_section9_rules_have_zero_false_positives` — the last reporting SYSTEM_TAG_NOT_IN_REGISTRY and REGISTRY_NAME_UNBOUND_MASTER against hhemt and agentic-workspace planning docs. A permanently-red suite trains readers to stop reading it, which is the condition that hides a real regression. — remedy-paths: tests/audit/test_config.py, tests/audit/test_decorator_registry_matches_baseline.py, tests/audit/test_scope_bucket_membership.py, tests/audit/test_section9_rule_soak.py, scripts/audit/_rules.py; engaged-set: scripts/audit/checks/_naming.py, scripts/audit/checks/_planning_hygiene.py, tests/audit/test_frontmatter_path_validity.py, tests/audit/test_rules_planning_hygiene.py, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3 hardening.md
### Specialist-identified follow-up items

#### agentic-workspace-specialist

- **The angle-bracket PreToolUse hook makes a byte-verbatim VMS impossible for any target file containing an unbackticked angle bracket.** The hook does not exempt fenced blocks, so a `Current text` block reproducing such a file is unwritable. Both workarounds are lossy: substituting braces fails `check_plan_anchor_freshness` with `diff-old-string-not-found`; narrowing the quotation produces a spec whose replacement scope exceeds its quoted anchor. Open question: how many prompt docs carry unbackticked angle brackets in fenced blocks, and should the hook exempt content inside a `Current text` / `diff` VMS region. This is a VMS-authoring-system defect, not a PWI defect.
- **`git_stage.py --commit` accepts paths only via `--paths`, and its usage line does not make that discoverable.** The natural `--commit {path} --message {msg}` invocation fails with `unrecognized arguments` rather than a directed message. Given git_stage.py is the sole sanctioned index mutator and is invoked from prose across the corpus, a one-line `--commit` help string naming `--paths` would remove a recurring first-attempt failure. (Adjacent: the commit-message detector's rejection message names the grammar, the type list and the 72-char cap, and is the model the `--commit` argument error is not.)

#### software-engineering-specialist

Routed from the `software-engineering-specialist --purpose plan-review` scratch (`2026-08-15_1300_software-engineering-specialist_plan-review.md`) at Step 6 item 4. F-B and F-I findings are NOT here — they roll into the plan docs.

**From `### Follow-up Ideas` (3 items, each dual-orthogonality tested by the specialist):**

- Add the per-property MAPI fallback map to the MSG reader so Exchange-internal `.msg` files without `PR_TRANSPORT_MESSAGE_HEADERS` promote instead of declining. The five property tags and types are already enumerated in the plugin's `docs/architecture.md` § Format support — `PR_SUBJECT` 0x0037, `PR_SENDER_NAME` 0x0C1A, `PR_SENDER_EMAIL_ADDRESS` 0x0C1F, `PR_CLIENT_SUBMIT_TIME` 0x0039 (FILETIME), `PR_INTERNET_MESSAGE_ID` 0x1035 — and the Phase-2 CFB reader is the only prerequisite. Post-publish; v1 declines these by design.
- Design the `.mbox` import-many flow as a second entry point over the promoter this plan builds. The architecture excludes `.mbox` as a different UX, but the promoter is most of the work and no design is recorded, so the exclusion reads as permanent rather than deferred. Two open questions to settle first: one attachment file per message vs. one archive attachment, and the batch size at which the Notifier-observer path stops being the right host (a 5,000-message archive is not a drop).
- `check_citation_integrity` cannot cite a planning doc and nothing warns an author before they try. `_is_valid_citation_target` admits `library/knowledge/` docs, recognized code files, cross-repo `{workspace-key}:` targets, and external URLs; a `library/docs/planning/...` target returns a blocking `INVALID_TARGET`. The rule is defensible (planning docs move to `completed/`, so the footnote would rot) but is discoverable only by reading the checker source after authoring the footnotes. Two candidate fixes: extend the `INVALID_TARGET` detail string to name the dual-syntax prose-provenance pattern as the sanctioned alternative, or state the target-class list in `citation format reference.md` where an author reads it beforehand.

**From `### Instructional Document Observations` (3 items):**

- `library/prompts/instructions/protocols/planning frontmatter schema.md` — `[ADD]` — § `new_files` never states what "does not exist on disk yet" means on a `phase` doc in a multiphase plan: not-yet at plan-authoring time, or not-yet at the start of this phase. The plan under review answers it inconsistently in BOTH directions (Phase 3 declares four files as new that Phases 0–2 create; Phase 1 declares `src/intake.js` as new that Phase 0 creates). Proposed one-sentence addition: "On a `phase` doc the field is **phase-relative** — it lists paths that do not exist on disk at the START of this phase, so a file an earlier phase creates is `affected_files` here and not `new_files`." Routed as an observation rather than a proposal because frontmatter-field semantics are agentic-workspace domain, not SE domain.
- `library/prompts/templates/atomic/body plan implementation phases.md` — `[ADD]` — two additions to § `#### Validation Plan`, both written out in full with verbatim assertion text in the specialist's `## Instructional Improvement Proposals` (see the two items below).
- `library/prompts/instructions/protocols/citation format reference.md` — `[ADD]` — the doc specifies the Pattern-A grammar but does not enumerate the four target CLASSES the checker accepts, so an author writes a grammatically perfect footnote and gets a blocking `INVALID_TARGET`.

**From `## Instructional Improvement Proposals` (2 proposals — these are ecosystem-level planning-instruction improvements that outlive this plan):**

- **Threshold-falsifiability tripwire** for `body plan implementation phases.md` § `#### Validation Plan`. The atomic already carries an Executability tripwire (can the command RUN); nothing governs whether an assertion can FAIL on the thing it names. This plan supplies the worked case: a superlinearity bound `t2 < 4*t1 + 5` where the named failure mode (repeated string concatenation while unfolding) is quadratic, which produces exactly 4x on a doubled input — so the assertion is executable, runs green, reads rigorous, and is mathematically incapable of failing on its target. Proposed verbatim tripwire text is in the specialist scratch; it requires, per numeric bound, the line "Threshold {name}: catches {failure mode}, which would produce {value}; bound is {bound}, which excludes it."
- **Extend the write-set-locality tripwire to READ targets in fenced file bodies**, same atomic and section. The existing tripwire covers files a phase CREATES or MODIFIES; it does not cover a path a plan names inside a fenced file body it authors verbatim, and it does not cover READ targets. This plan hit both at once (`const dir = path.join(__dirname, 'fixtures', 'pathological')` inside an authored test body, reading a directory no phase creates and the master separately records as withdrawn). Proposed verbatim extension is in the specialist scratch.

**From `#### Term Gaps` — routed in full per the Term-Gaps carve-out, which requires every enumerated term to appear here even when the specialist dismissed it as covered or common.** The specialist's own classification is preserved inline as the teaching pointer. Note: the specialist counted "eleven candidate terms" but named sixteen; all named terms are routed and the count discrepancy is recorded rather than resolved.

- **Notifier observer priority** — specialist note: Zotero-platform vocabulary, `zotero-specialist` domain, not SE
- **`Zotero.DB.executeTransaction`** — specialist note: Zotero-platform vocabulary, `zotero-specialist` domain
- **`registerObserver`** — specialist note: Zotero-platform vocabulary, `zotero-specialist` domain
- **ZotMoov automove** — specialist note: Zotero-platform vocabulary, `zotero-specialist` domain
- **Better BibTeX cite-key pinning** — specialist note: Zotero-platform vocabulary, `zotero-specialist` domain
- **`PR_TRANSPORT_MESSAGE_HEADERS`** — specialist note: Zotero-platform / MAPI vocabulary, `zotero-specialist` domain
- **Compound File Binary (CFB)** — specialist note: Zotero-platform / container-format vocabulary, `zotero-specialist` domain
- **RFC 2047 encoded-word** — specialist note: message-format vocabulary; belongs with the RFC substrate the architecture's residual 4 records as missing, rather than as a free-standing term file written from plan prose
- **header unfolding** — specialist note: message-format vocabulary, same disposition as above
- **folded continuation line** — specialist note: message-format vocabulary, same disposition as above
- **superlinearity** — specialist note: general SE vocabulary, already carried in SE substrate
- **ReDoS** — specialist note: general SE vocabulary, already carried in SE substrate
- **catastrophic backtracking** — specialist note: general SE vocabulary, already carried in SE substrate
- **characterization test** — specialist note: general SE vocabulary, already carried in SE substrate
- **seam** — specialist note: general SE vocabulary, already carried in SE substrate
- **deep module** — specialist note: general SE vocabulary, already carried in SE substrate

**From `### Code-Level Observations` — one substrate gap worth routing (the scratch carries no `### Substrate Gaps` section by composition, so the specialist recorded it here):**

- The plugin's `docs/architecture.md` § Known residuals item 4 records three open substrate gaps — no `zotero-plugin-template` clone, no RFC 5322 / 2047 / 2231 / 2392 reference, and no `[MS-CFB]` / `[MS-OXMSG]` extraction — and NO phase closes the residual. The RFC gap is the one that bites: Phase 2's fixture expectations encode RFC 2047 semantics (adjacent-encoded-word whitespace elision, `_`-as-space in Q-encoding) that an implementer will otherwise reconstruct from the fixtures themselves, which is circular. Extracting RFC 2047 and RFC 5322 §2.2/§3.6 into substrate before Phase 2 is cheap and would make the fixture expectations checkable against a source rather than against the plan's own prose.
- Related: residual 3 (withhold-on-differ as the common branch) and residual 4 are tracked by no phase, so the architecture's residual list will still read as open after Phase 4 marks the design shipped.

- [friction-followup] The satellite model and the compaction-gate machinery disagree about where PWI preflight state lives, and it cost three separate blocks in one gate. `satellite routing.md` sends the Common-Prefix preflight record to the `pwi-preflight` satellite, but three consumers read the MAIN scratch and cannot see a satellite: (1) `write_compaction_sidecar --mode create` ports `## Worktree Status` from main and HARD-REFUSES without a resolvable `worktree-{slug}` line — while its own wrapup path (`_render_wrapup`) SYNTHESISES a fallback body for exactly that absence, so the two modes disagree; (2) `check-compaction-gate-before-edit.sh` requires a `## Post-Preflight Compaction Gate` section in main and is NOT heading-level tolerant — an H1 fails it, and `append_scratch_section --header` emits H1, so the natural actuator produces a heading the natural consumer rejects; (3) the same hook's predicate additionally requires the LITERAL strings `Assertion A` and `Assertion B`, which the protocol's own Step-1 format supplies only in its bold labels — recording the stem sentences alone ("Specialist reviews performed this session: ...") passes the protocol and fails the hook. Measured 2026-08-16 on the first compaction of a cross-repo PWI; each surfaced as a separate refusal and each needed a separate remedy in the main scratch. Candidate fixes: give `--mode create` the same fallback the wrapup path has; make the hook heading-level tolerant or make `append_scratch_section` able to emit H2; and state in `pwi post preflight compaction gate.md` Step 1 that the literal labels are load-bearing rather than presentational. — remedy-paths: scripts/generate/write_compaction_sidecar.py, scripts/hooks/check-compaction-gate-before-edit.sh, scripts/generate/append_scratch_section.py, library/prompts/instructions/protocols/pwi post preflight compaction gate.md, library/prompts/instructions/protocols/satellite routing.md; engaged-set: the zotero-email-intake plan family, scripts/audit/checks/_naming.py, scripts/audit/checks/_planning_hygiene.py
#### Session d59b1e56-6067-4244-ba36-def3a0de5463
- [friction-followup] `affected files convention.md` has no exclusion class for work the USER scopes in AFTER a phase plan is authored, so the PWI stop gate's Condition 1 fires on it as a scope violation. Measured at the Phase-0/Phase-1 boundary: nine files (a pre-commit config, seven hook scripts, an in-flight sidecar) were correctly landed as three separately-scoped commits with the phase commit still carrying exactly its four declared files, and Condition 1 still triggered because the match is path-exact against `affected_files:` and none of the three documented exemptions (sentinel-only, planning-table, convention-excluded) covers the class. The gate is behaving as written; the convention is missing a class. Any fix must NOT weaken the path-exact match, which is what makes the condition catch real scope creep — remedy-paths: library/prompts/instructions/protocols/affected files convention.md, library/prompts/instructions/skills/proceed-with-implementation/stop gate.md; engaged-set: README.md (zotero-email-intake), library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/zotero-email-intake-plugin master.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/implemented/0 scaffold repair and loader adoption.md, scripts/generate/create_specialist_call.py
- [friction-followup] `check-compaction-gate-before-edit.sh` reads the MAIN scratch only and cannot see a satellite, so a PWI whose `### Post-Preflight Compaction Gate` lives in its `pwi-preflight` satellite is hard-blocked on EVERY non-scratch Write for the rest of the session. Measured this session; the live workaround was an H2 pointer section in the main scratch reproducing only the literal `Assertion A` / `Assertion B` strings the hook greps, verified hash-safe because the section sits outside WRITABLE_SECTIONS. That workaround is load-bearing and must not be deleted while it is the only unblock. Two candidate architectures and the choice between them is NOT settled: (a) teach the hook to resolve satellites through the main scratch's link index, which keeps one authoritative copy but gives a pre-commit-adjacent hook a new dependency on scratch link-index structure; (b) make satellite composition mirror gate assertions back into main by contract, which keeps the hook simple but creates a second copy that can drift from the satellite's authoritative one. Root cause is the satellite/hook split having no governing doc at all — remedy-paths: scripts/hooks/check-compaction-gate-before-edit.sh, library/prompts/templates/atomic/pwi preflight satellite body.md, scripts/generate/compose_satellite.py; engaged-set: README.md (zotero-email-intake), library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/zotero-email-intake-plugin master.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/implemented/0 scaffold repair and loader adoption.md, scripts/generate/create_specialist_call.py
- [friction-followup] The default merge subject git generates is rejected by this repo's own commit-msg hook, so the integration SOP's prescribed merge command cannot succeed. **Measured**: `pwi pre compaction main integration.md` Step 4 prescribes a no-edit merge against `origin/$TRUNK`; the subject git generates (`Merge remote-tracking branch 'origin/main' into worktree-...`) fails `check-commit-message` at exit 1 with `missing ': ' separator`. Step 5's no-edit completion command fails identically. Unconditional rather than intermittent — the subject is a fixed string the SOP does not control, so every merge in this repo hits it, including worktree closeout and `/git-tree-cleanup`. Worked around by passing an explicit message carrying a `chore:` subject. **Root cause is established; only the fix SHAPE is open**: `scripts/hooks/_commit_message_detector.py` contains NO merge handling at all, while `scripts/hooks/check-commit-message.py` DOES carry merge awareness — a `MERGE_HEAD` existence test guards `validate_claim_deletion`, with the stated reason that merge commits legitimately carry claim deletions and cannot carry the release subject. So merge-awareness is present in the hook and was applied to exactly one rule. That asymmetry is why this reads as an oversight with an in-repo precedent for its own remedy, rather than a policy that merges must conform. **Candidate A, hook-side**: skip the Conventional Commits grammar check while `MERGE_HEAD` exists, mirroring the existing carve-out. Cheap, single-point, and the Conventional Commits spec itself treats merge commits as outside its grammar — but it widens what the repo accepts, and nothing announces that merge subjects were ever meant to be classifiable. **Candidate B, SOP-side**: prescribe an explicit conforming message at every merge site. Keeps the gate fail-closed, but repairs one hook defect across N prose sites and a site missed still fails; it also reverses the purpose of the no-edit form, which exists to stop an agent describing a merge it has not inspected. **The one unverified fact that decides between them**: whether anything in this repo consumes merge-commit subjects (changelog or release tooling). If nothing does, Candidate A is correct and the case against it evaporates. Check that before choosing. **Secondary observation, no action proposed**: the repo's own write guard false-positives on prose describing git commands — this bullet could not be passed via `--content` because the guard's pattern matched the no-edit flag inside the payload text; `--content-file` is the already-prescribed path and was used. — remedy-paths: scripts/hooks/_commit_message_detector.py, scripts/hooks/check-commit-message.py, library/prompts/instructions/protocols/pwi pre compaction main integration.md, library/prompts/instructions/protocols/worktree closeout.md; engaged-set: README.md (zotero-email-intake), library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/zotero-email-intake-plugin master.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/implemented/0 scaffold repair and loader adoption.md, scripts/generate/create_specialist_call.py

- [halt-record, RESOLVED] Post-compaction resume halted at the First Action conjunctive GO gate — halt trigger #5, Condition 2 — on sidecar `sidecar_phase1_2026-08-16_1501.md`, now archived to `sidecars/done/`. Parts 1, 2 and 3 all returned GO (`--mode verify` exit 0, worktree clean); Conditions 1, 3 and 4 PASSED; Condition 2 FAILED because `### Phases Remaining` carried the generic not-present placeholder instead of a numbered list. **Cause was not plan state.** The sidecar was a correct rendering by a SUPERSEDED module: `PYTHONPATH=$AGENTIC_WORKSPACE` resolves `scripts.generate.write_compaction_sidecar` to the AW main tree, which was 3 commits behind `origin/main` and therefore lacked `8e7063af20 fix(compaction-system): derive Phases Remaining when absent from main` — a commit this same session authored and landed 13 h 22 min earlier. **Resolved** by re-emitting via `--mode create` with `PYTHONPATH` pointed at the AW worktree copy, measured byte-identical to `origin/main`. The replacement sidecar's `### Phases Remaining` derives correctly from the master `## Phase Status` table with phase 0 filtered out and phase `3b` retained. No sync-back was run at any point: no phase completed this session, so `_try_auto_advance_next_phase` would have returned `"unchanged"` and copied the prior `leave_off_step` into a fresh sidecar. Full triage: the `handle-friction` satellite, round `## stale-main-tree-sidecar-module — 2026-08-16 15:16`.
- [friction-followup] **Worktree PWI sessions execute agentic-workspace scripts from an unversioned main tree, so a landed fix stays invisible until someone pulls, and nothing gates a PWI on main currency.**
  **Measured.** A post-compaction resume halted at halt trigger #5 Condition 2 because the in-flight sidecar's `### Phases Remaining` was a placeholder rather than a numbered list. The code defect that produces that placeholder — an ABSENT `### Phases Remaining` taking the generic not-present prose instead of the empty-body path, so `_is_empty_phases_remaining_body` returns False and `_derive_phases_remaining_from_master` never fires — had already been fixed on `origin/main` by `8e7063af20 fix(compaction-system): derive Phases Remaining when absent from main`, committed `2026-08-16 01:39:18 -0400`. The sidecar was rendered `2026-08-16T15:01:57`, 13 hours 22 minutes later, and still carried the pre-fix behaviour. The AW main tree measured 13 ahead / 3 behind `origin/main`; the same commit also carried the string-keyed `_audit_completed_phase_ids` conversion that lets a non-integer phase id such as this plan's `3b` filter correctly, so a second latent defect was equally invisible.
  **Mechanism.** The corpus mandates `PYTHONSAFEPATH=1 PYTHONPATH=$AGENTIC_WORKSPACE` on every script invocation issued from a worktree. That form exists for a good reason and achieves it: `PYTHONSAFEPATH=1` suppresses the cwd auto-prepend so `python -m` cannot resolve `scripts.*` into a project workspace's own top-level `scripts/` package. But it also pins execution to whatever the AW MAIN TREE happens to hold, and the main tree is versioned against nothing — it can sit arbitrarily far behind `origin/{trunk}` while every gate, generator, and validator in a PWI runs from it. The failure is silent in both directions: the stale module produces a well-formed artifact, and the gate that consumes it reports a condition failure attributable to plan state rather than to code version.
  **Detection exists but is purely advisory.** The SessionStart hook printed `58 repo(s) behind upstream (cached check) — run /sync-workspaces to review and pull.` Nothing gated on it, and the notice does not distinguish agentic-workspace — the repo whose code every session executes — from the 57 others.
  **Why the existing guard did not catch it.** `_readonly_sections_unresolved` is a create-time guard whose stated purpose is refusing to write a sidecar that would trip halt #5 at resume. Its Condition-2 arm matches three substrings, all emitted by `_derive_phases_remaining_from_master`: `cannot derive Phases Remaining`, `no \`## Phase Status\` table in master plan`, `no master plan resolvable`. The absent-section placeholder is none of them, so the guard passed and the sidecar was written. The landed fix cures this as a side effect — absence now reaches the derivation, which either produces a real list or emits a substring the guard recognizes — but the general shape stands: a guard keyed on a producer's sentinel vocabulary is blind to any path that bypasses that producer.
  **Candidate remedies, none selected.** (1) Gate PWI start on the AW main tree being level with `origin/{trunk}`, in the Common Prefix beside the existing worktree and claim checks. (2) Have generators self-check their own blob against `origin/{trunk}` and warn on divergence, which localizes the check to the artifacts whose staleness actually corrupts state. (3) Re-point `PYTHONPATH` at the AW WORKTREE for AW-script invocations during a PWI, and state the rule once in `scratch doc writing norms.md` beside the existing pytest carve-out, which already establishes that the `PYTHONPATH` root is contextual rather than universal. Option 3 is the cheapest and the most likely to drift, since it depends on every prose call site being updated; option 1 is the only one that fails closed.
  **Not selected because the choice needs design work that outlives this round** — the three options differ in where they fail, not merely in cost, and option 1 changes PWI start semantics for every workspace.
  — remedy-paths: library/prompts/instructions/skills/proceed-with-implementation/pwi common prefix.md, library/prompts/instructions/norms/scratch doc writing norms.md, scripts/generate/write_compaction_sidecar.py, scripts/setup/repair.sh, scripts/utils/paths.py; engaged-set: the agentic-workspace main-tree ref at /home/dcl3nd/dev/agentic-workspace (advanced to origin/main by this round's S1), /home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi/sidecars/sidecar_phase1_2026-08-16_1501.md and its re-emitted replacement (this round's S2), and the active plan's affected_files — src/intake.js, src/message.js, src/probe.sys.mjs, locale/en-US/emailintake.ftl, bootstrap.js, docs/architecture.md
  **Sharpest available statement of the defect, measured after the bullet above was written.** `8e7063af20` is an ANCESTOR of this session's own worktree HEAD (`git merge-base --is-ancestor 8e7063af20 HEAD` exits 0 in the AW worktree), and this satellite carries a prior round of this same session, `## phases-remaining-absent-branch-halts-resume — 2026-08-16 01:23`, whose disposition produced it — the commit landed 16 minutes later at 01:39. So THIS SESSION diagnosed the defect, authored the fix, and landed it on `origin/main`; then, because every scripted invocation on the resume path is rooted at `PYTHONPATH=$AGENTIC_WORKSPACE`, it continued executing the pre-fix module and halted on the same defect 13 hours 22 minutes afterwards. A session cannot see its own landed fix. That is the failure in its strongest form, and it rules out the reading that this is merely a housekeeping lapse about pulling often enough: the fix was already on the integration branch, produced by the very agent that then could not observe it.
- [friction-followup] **The stale-main write guard routes an INBOUND sync to an OUTBOUND-only landing procedure, leaving a legitimate operation with no agent-executable path.**
  **Measured.** From this PWI worktree session, `git -C /home/dcl3nd/dev/agentic-workspace merge origin/main -m {conforming-message}` — a single-clause command with an absolute target, on a main tree measured CLEAN (`classify_main_tree_dirty` reported 0 safe-to-autocommit and 0 hand-authored) with no suspension sentinel held — was REFUSED with: "a write to AW main during a PWI is valid ONLY through a full-fidelity application of /handle-friction -- run it and follow its landing procedure." The refusal is correct on its own terms and is not a misclassification: the guard's text states that for an ABSOLUTE target "an absolute genuine write still blocks when split", and the command was already single-clause.
  **Why the prescribed remedy does not fit.** `handle friction SKILL.md` § Landing a systematic fix on its integration branch is entirely OUTBOUND: resolve `$TRUNK` fail-closed, obtain a discrete validated `FIX_SHA` on a worktree branch, mirror it onto `origin/$TRUNK` through an ephemeral worktree, reconcile the source worktree. Every step presumes a fix travelling FROM the session TO the integration branch. The operation refused here is the exact inverse — an inbound merge of three commits ALREADY on `origin/main`, one of which this same session authored and landed. There is nothing to author, nothing to cherry-pick, and nothing to push, so no step of that procedure applies and following it "in full fidelity" terminates without performing the operation.
  **Consequence.** The guard's message asserts that a valid path exists ("valid ONLY through ... its landing procedure"), and for this operation class there is none. An agent that trusts the message will either loop through a procedure that cannot apply or, worse, read the absence of an applicable step as licence to bypass the guard. The only actuator left is the user, from a main-cwd session — which is a fine answer, but it is not the answer the guard gives.
  **Adjacent, same round, worth recording with it:** `git pull` in this git build accepts no `-m`, so the natural "pull with a conforming merge message" form fails at exit 129 with `unknown switch`. Since this repository's `commit-msg` hook rejects git's default merge subject (already captured as a separate follow-up this session), the working form is `git fetch` followed by `git merge -m {conforming}`. Any prose site that prescribes "pull with an explicit message" as the workaround for the merge-subject defect is prescribing a command that cannot run.
  **Candidate remedies, none selected.** (1) Give the guard an inbound-sync arm that names the actual actuator — `/pull-agentic-workspace` from a main-cwd session — instead of pointing at the landing procedure. (2) Allowlist a narrowly-shaped `git -C {aw-main} merge origin/{trunk}` when the main tree is measured clean and unsentinelled, on the same reasoning that already admits the landing actuator. (3) Leave the guard alone and correct only its message, which is the cheapest and does not widen what the repo accepts.
  — remedy-paths: scripts/hooks/ (the stale-main write-guard hook and its refusal text), library/prompts/instructions/skills/handle-friction/handle friction SKILL.md (§ Landing a systematic fix on its integration branch — scope statement), library/prompts/instructions/protocols/pwi pre compaction main integration.md; engaged-set: the agentic-workspace main-tree ref at /home/dcl3nd/dev/agentic-workspace (this round's S1 target, left unadvanced), /home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi/sidecars/sidecar_phase1_2026-08-16_1501.md and its re-emitted replacement (this round's S2), and the active plan's affected_files — src/intake.js, src/message.js, src/probe.sys.mjs, locale/en-US/emailintake.ftl, bootstrap.js, docs/architecture.md
- [friction-followup] **A specialist finding can specify a code change, be accepted, and reach NO phase doc's File-by-File — the plan family then states the requirement in prose and its negation in the entry that governs the edit.**
  **Measured.** The `zotero-email-intake-plugin` master carries, inside `### Implementation Research` → `#### software-engineering-specialist`, a Round-6 "Finding 2" inventory row and its Round-7 "Restated Finding-2 Phase-0 inventory rows" correction. Both specify that `bootstrap.js` must `loadSubScript` BOTH `src/intake.js` and `src/message.js` (master:428 "add `loadSubScript(rootURI + "src/…")` for `intake.js` and `message.js`"; master:484 "`loadSubScript` for `src/*.js`"). Neither phase doc's File-by-File ever carries it. Phase 0's doc is single-file throughout — its constant-rename table row 13 maps `mainJS` to `"src/intake.js"`, and its binding-contract paragraph governs `src/intake.js` alone. Phase 1's `bootstrap.js` entry states the negation outright: "No other line is touched." The live master body is silent too: Phase Strategy assigns Phase 0 "the placeholder `src/intake.js`" and Phase 1 "a stub `src/message.js`", with no loader call in either.
  **Consequence.** `src/message.js` is created by Phase 1 and never evaluated. The requirement survives only as PROSE inside a different entry of the same phase doc ("`bootstrap.js` loads `src/message.js` ahead of `src/intake.js`"), which no applying agent treats as a spec because it sits in the `src/intake.js` entry and names a file that entry does not own. Detected only at Implementation, by reading the loader against the entry that forbids editing it.
  **Why this is a class, not an instance.** The roll-in direction is specialist-finding to phase-doc File-by-File, and nothing verifies that a finding's every actionable clause arrived. A finding whose clauses span TWO files — here `bootstrap.js` and `src/message.js` — can be rolled in for one and dropped for the other, and the result reads complete because the file that WAS rolled in is present and correct. The `AFFECTED_FILES_UNDERCOLLECTED` rule the master itself cites (master:477) catches a File-by-File row naming a path the frontmatter omits — the opposite direction, and blind to this one: here the frontmatter DOES list `bootstrap.js`, and it is the File-by-File CONTENT that is short.
  **What would have caught it.** A per-finding roll-in receipt enumerating each actionable clause against the phase-doc entry that now carries it, with the count asserted rather than the arrival assumed. Related but insufficient: `check_plan_anchor_freshness` reported an `affected-files-mismatch` WARN on this very phase doc naming five paths with "no File-by-File entry" — including `bootstrap.js` — but that WARN is a parse artifact (the doc writes entries as `` `path — code` `` headers the collector does not recognize) and it fires identically whether an entry is missing or merely unparsed, so it cannot discriminate this defect from its own false positives.
  **Candidate remedies, none selected.** (1) Extend the specialist plan-review roll-in protocol with a per-clause receipt naming the destination entry for each actionable clause. (2) Teach `check_plan_anchor_freshness` to collect the `` `path — tag` `` File-by-File header form, which would turn its current blanket WARN into a real membership check and make the affected-files direction meaningful. (3) Add a phase-doc self-check that every path in `affected_files:` has a File-by-File entry whose body names at least one concrete edit — the inverse of `AFFECTED_FILES_UNDERCOLLECTED`.
  — remedy-paths: library/prompts/instructions/protocols/specialist plan review roll in.md, scripts/validate/check_plan_anchor_freshness.py, scripts/audit/checks/_planning_hygiene.py, library/prompts/templates/atomic/body plan implementation phases.md; engaged-set: /home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi/bootstrap.js (this round's VMS 1), the Phase 1 doc's `bootstrap.js` File-by-File entry (this round's VMS 2), and the active plan's affected_files — src/intake.js, src/message.js, src/probe.sys.mjs, locale/en-US/emailintake.ftl, bootstrap.js, docs/architecture.md
- [friction-followup] **The corpus governs which arms a gate may NOT offer and never which it MUST. An arm set narrower than the gate's own declared acceptable responses passes every check, and the user pays for it by rejecting the tool call.**
  **Measured.** A `/handle-friction` Step-2 gate was emitted as an `AskUserQuestion` with two arms, both approvals ("Approve, Option B list" / "Approve, Option A scalar"). Its own checkpoint-context declares four acceptable response classes verbatim: "explicit approval of the dispositions, another round of handle-friction on residual issues ..., clarification on friction resolution objectives, decisions to re-route some or all of the dispositions to followups." Three of the four had no selectable arm. The user rejected the tool call and re-invoked `/handle-friction` — exercising precisely the class that had been omitted.
  **Why every existing check passes it.** `arm license test.md` is subtractive by construction: Tier 1 licenses the QUESTION, Tier 2 licenses EACH ARM and strips the deferral arm and the dominated arm. Both tiers remove arms; neither asks whether the surviving set SPANS the decision. Each of the two arms emitted had a real consequence, a real license, and a stateable reason a reader might prefer it, so the test returned clean on a set that could not express three quarters of the gate's own contract. `interactive checkpoint.md` fixes the TOOL for an enumerated choice and sanctions bundling independent decisions, and states no coverage obligation. The failure is therefore invisible to the one document written to police arm sets.
  **Why it is not merely cosmetic.** `Other` is auto-appended, so a response is never impossible — but the user must either type free text that binds to no arm or reject the call, and a rejection under Continuation Rule 6 is a REDIRECT that RETIRES the gate. So the omission does not degrade the interaction, it destroys the decision: the approval the gate existed to obtain is lost, and a fresh round must be run to re-fire it. The cost is one full round per occurrence.
  **The asymmetry worth naming.** The corpus invested heavily against over-offering because that failure is visible — a menu with an obviously wrong option reads as sloppy. Under-offering reads as DECISIVE, which is why it survives self-review: an agent that narrows a gate to two well-argued arms feels like it has done the analytical work, and the arms it silently dropped leave no trace in the emitted artifact. The self-review question "did I offer a bad arm?" is answerable from the call; "did I omit a licensed one?" is only answerable against the checkpoint-context, which nothing directs the agent back to at emission time.
  **Two candidate placements, and the argument that decides between them.** (1) `library/prompts/instructions/protocols/interactive checkpoint.md`, beside the sentence that already routes enumerated choices to `AskUserQuestion`. (2) `library/prompts/instructions/protocols/arm license test.md`, as a Tier-3 counterpart to its two subtractive tiers. Placement (1) is preferred on one ground: the obligation is stated in checkpoint-context vocabulary ("the acceptable-response classes this gate's checkpoint-context names"), which is that file's own contract, and adding an ADD-arms rule to the arm-license test inverts its rhetorical direction mid-document and risks readers citing coverage to manufacture the arms Tier 2 exists to strip. A one-sentence form keyed to an artifact the agent already has open is checkable rather than exhortatory — the emitting agent can compare its arm labels against the enumerated classes without further reading.
  **Not selected because the placement is genuinely contested and the immediate cost was paid another way** — the round that surfaced this discharged it by re-firing the gate with arms spanning approve / re-route / another-round, which is the remedy for the instance but not for the class.
  — remedy-paths: library/prompts/instructions/protocols/interactive checkpoint.md, library/prompts/instructions/protocols/arm license test.md; engaged-set: the Phase 1 doc's `bootstrap.js` File-by-File entry and /home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi/bootstrap.js (the two act-now targets carried from the prior round), and the active plan's affected_files — src/intake.js, src/message.js, src/probe.sys.mjs, locale/en-US/emailintake.ftl, bootstrap.js, docs/architecture.md
##### zotero-specialist
Routed from the `zotero-specialist --purpose implementation-friction-research` scratch (`2026-08-16_1553_zotero-specialist_implementation-friction-research.md`, topic `message-js-module-loading`) at Step 6 item 4. The `### Research Output` deliverable is NOT here — it is ported under `### Friction Resolution: message-js-module-loading` in this scratch and sits behind a confirm-then-apply gate.
- **`CODE_CITATION_EXTENSIONS` omits `.mjs` and `.sys.mjs`, so post-ESMification Mozilla-platform source cannot be footnoted.** `scripts/utils/citation_format.py` defines the set as `{.py, .sh, .js, .ts, .yaml, .yml, .toml, .ini, .cfg, .json, .c, .h, .cpp, .hpp}`. Zotero 8 converted every JSM to an ESM, so `.mjs` and `.sys.mjs` are now the dominant JavaScript extensions across the cloned client — `chrome/content/zotero/zotero.mjs` and the whole `pluginAPI/*.mjs` surface among them. Three otherwise-valid, quote-verified footnotes in this session's knowledge doc were rejected as `INVALID_TARGET` purely on extension, forcing the evidence to be inlined as a fenced quotation with no machine verification. Adding `.mjs`, `.sys.mjs`, `.cjs`, `.jsx`, `.tsx` to the frozenset would close it.
- **`check_plan_anchor_freshness` cannot grade a VMS whose target is a JavaScript file.** `_PATHY_SUFFIX` is `\.(py|md|ya?ml|toml|json|txt|sh|cff|cfg|ini|css)$` — `.js` is absent, so any `.js` target reports `target-unresolvable: not a file path` and escalates to `FAIL: target-not-found`, regardless of repo. The discriminator is the EXTENSION, not the repo: `.json` and `.md` targets in the same project repo resolve fine. Any plan speccing an edit to a JavaScript file therefore can never reach exit 0, which trains readers to discount the gate — and JavaScript is this project's entire source language. **Independently confirmed by the main agent this session**: the gate returned FAIL on both `bootstrap.js` specs across all four specialist rounds while every plan-doc spec passed, and `normalize_vms_path` rejects `src/intake.js` identically. Adding `js|mjs|cjs|jsx|ts|tsx` would close it. **Pairs with the item above — two separate validators, both with hand-maintained extension allowlists that have fallen behind the corpus's languages, which suggests one shared extension registry as the durable fix rather than two independent patches.**
- **The `await`-on-a-synchronous-call pattern recurs across this substrate and there is no lint for it.** ZotMoov's `ScriptLoader.loadScript` writes `await Services.scriptloader.loadSubScript(...)`; the Phase-1 ESM spike writes `await ChromeUtils.importESModule(...)`; Zotero core awaits neither, at zero of 132 `importESModule` sites. Neither instance is a bug, but each invites the belief that the `await` supplies sequencing — the belief that makes a later refactor drop it and appear to change nothing. A small ESLint rule, or a grep in the Phase-3 invariant suite, over a known-synchronous call list would catch the class. The SPECIFIC Phase-1 instance is in-scope and already addressed by a live spec that documents the token rather than deleting it; what is orthogonal is the general rule.
**From `### Substrate Gaps` (3 gaps, routed unconditionally per Step 6 item 4; the parallel `/manage-substrate` offer is a separate synchronous path):**
- Missing: `windingwind/zotero-plugin-template`, the canonical small-plugin reference — Type: `clone` — Reference: https://github.com/windingwind/zotero-plugin-template. Already logged as substrate gap 1 in the specialist's identity known-gaps list and as residual 4 in the plugin's `docs/architecture.md`, and it bit here: the loader-shape survey rested on three donors (Attachment Scanner, ZotMoov, Zotero core), and the template is the fourth data point that would corroborate the list shape or supply a fifth option. The answer stands without it — Zotero core's own `xpcomFilesAll` is the stronger precedent — but a fourth donor would have made the MECE claim measured rather than argued.
- Missing: authoritative documentation for `mozIJSSubScriptLoader` scope semantics — Type: `web-cache` — Reference: https://searchfox.org/mozilla-central/source/js/xpconnect/loader/mozJSSubScriptLoader.cpp. Two specific unknowns: whether `loadSubScript` with no `target` argument compiles for the syntactic or a non-syntactic global (which decides whether a top-level `let`/`const` in a subscript is even visible to a later subscript), and whether the subscript-loader cache is consulted for a plugin's `file://` and `jar:` rootURI. Both are answered indirectly in the deliverable — the first is made moot by the `var`/`function` rule required for an independent reason, the second is recorded as an unverified diagnosis — but neither was settled from source. The loader is Firefox platform code and is in no cloned repo in this workspace.
- Missing: a Zotero-side statement of ESM lifetime for plugin-loaded modules — Type: `web-cache` — Reference: https://www.zotero.org/support/dev/zotero_8_for_developers. Whether `ChromeUtils.importESModule` from a plugin `rootURI` is evicted on plugin upgrade or persists for the process. The specialist's finding that it persists is DERIVED (ESM modules are cached process-wide by URI; `Cu.unload` appears nowhere in the cloned tree post-ESMification) rather than read from a Zotero or Mozilla statement, and it is load-bearing for the recommendation that Phase 2 should not elect ESM — so it is flagged rather than asserted flatly.

- [friction-followup] The two sanctioned scratch actuators name the same argument differently, and the divergence costs a failed first invocation every time. `scripts/utils/insert_into_scratch.py` requires `--scratch`; `scripts/generate/append_scratch_section.py` requires `--scratch-doc`. Prose across the corpus prescribes both actuators in adjacent paragraphs — `scratch doc writing norms.md` § Edit-vs-Bash names them in one sentence, and `handle friction SKILL.md` prescribes `append_scratch_section` for the orthogonal-followup disposition and `insert_into_scratch` for newest-first insertion — so an agent that has just written one invocation carries the wrong flag into the next. Measured 2026-08-16: an `insert_into_scratch --scratch-doc ...` call exited 2 with `the following arguments are required: --scratch`, and the error names the required flag but not the alias that would have worked, so recovery is a re-read of the usage line rather than a directed fix. The failure is loud and cheap individually, which is why it persists; the cost is one wasted invocation per site and it recurs for every agent. Cheapest durable fix is an `--scratch-doc` alias on `insert_into_scratch`'s `--scratch` argument (argparse supports multiple option strings on one `add_argument`), which changes no call site and makes both spellings work on both actuators; the alternative — normalizing every prose call site to the correct per-script spelling — repairs N documents and a site missed still fails. — remedy-paths: scripts/utils/insert_into_scratch.py, scripts/generate/append_scratch_section.py; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/2 parser both formats and the test harness.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3 hardening.md, the src/message.js exported surface (detect / parseHeaders / mapToPayload) and src/intake.js's detection helpers that those edits establish as a contract, and the Phase 2 plan's 28 declared affected_files

- [friction-followup] The post-compaction-resume carve-out admits exactly three namers for a non-ported main-scratch section read, and a sidecar's own `custom_instructions:` is not among them — so an unconditional custom instruction to read such a section has no admissible form of the verbatim assertion. Measured 2026-08-17 on a Phase 3 resume: `custom_instructions` item 0 mandated a MANDATORY FULL READ of the main scratch's `# SESSION OPERATING MANDATE` H1 and designated it "item 0 of the Mandatory reads list", but the carve-out's three namers are the phase doc, the sidecar's `### Leave-off step`, and the Clear-Gate-Clearance guard. The read was performed and the assertion written with the namer stated honestly as `custom_instructions`, which is a fourth namer the form does not sanction. Nothing broke — the section sits outside `WRITABLE_SECTIONS`, the read was read-only, and `--mode verify` returned exit 0 afterward — so the defect is silent and recurs on every sidecar carrying a read-mandating custom instruction. The carve-out states its own repair shape and forecloses the lazy fix: "Adding a fourth namer requires naming a fourth specific guard, not relaxing the form." The candidate fourth guard is the `## Session Custom Instructions` block that `write_compaction_sidecar` renders into every sidecar body, which is specific, machine-emitted, and bounded in the same way the Clear-Gate-Clearance guard is. Note the competing reading that would make the amendment unnecessary: a custom instruction naming a section as a mandatory read arguably makes that section a MANDATORY READ rather than a carve-out read, in which case the carve-out never applies and the assertion is not owed at all — the two readings disagree about whether the assertion should have been written, which is itself the ambiguity worth closing. — remedy-paths: library/prompts/templates/atomic/post compaction resume preamble common.md, scripts/generate/write_compaction_sidecar.py; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3 hardening.md, src/intake.js, src/message.js, prefs.js, prefs.xhtml, locale/en-US/emailintake.ftl, package.json, scripts/ci/invariants.sh, .github/workflows/ci.yml, test/redos.test.js, docs/architecture.md, bootstrap.js
- [friction-followup] **`scripts.audit --files` with an ABSOLUTE path silently scans nothing and reports `0 errors`, which renders as a pass.** Measured 2026-08-17 at the Phase-3 diff-QC gate replay: `python -m scripts.audit --files "{absolute path to a planning doc inside an AW worktree}"` printed `**0** docs scanned | **0** errors | **0** warnings | **0** clean` and exited 0. The identical file audited from `cd {aw-worktree}` with a REPO-RELATIVE path printed `**1** docs scanned | **0** errors | **0** warnings | **1** clean`. The absolute form verified nothing while emitting the same `0 errors` a real clean run emits, and the only discriminator is the `docs scanned` counter — a reader who greps for `errors` or reads a summary line alone cannot tell the two apart. This is the vacuous-pass class the corpus already names for `check_plan_anchor_freshness`'s `0/0 target(s) resolved` (`parse-coverage-zero`), reached here through a different door: there the gate names its own vacuity, here it does not. Two candidate remedies: make `--files` fail closed (non-zero, or a loud `SCANNED 0 OF N REQUESTED` line) when the resolved file set is empty while paths were supplied; or normalize absolute paths against the resolved repo root before matching, so an in-repo absolute path resolves rather than silently falling out of the universe. The second is the better fix if the cause is root-relativization, because the invocation is natural and every prose call site that passes an absolute path is exposed. — remedy-paths: scripts/audit/__main__.py, scripts/utils/paths.py; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3 hardening.md, bootstrap.js, src/intake.js, src/message.js, prefs.js, prefs.xhtml, locale/en-US/emailintake.ftl, package.json, scripts/ci/invariants.sh, scripts/ci/run-tests.sh, .github/workflows/ci.yml, test/redos.test.js, docs/architecture.md
- [friction-followup] **A sidequest disk-change ledger's verify commands self-count against the row that quotes them, and the protocol states the hazard without giving the row an escape that always works.** `sidequest protocol.md` § The sidequest working record requires each ledger row to state a verification command that is "COLUMN-0 ANCHORED and region-scoped", and warns that "a whole-file substring count over the scratch also counts the ledger's own quotation of the string it checks". Measured THREE separate occurrences of that exact failure in one session, all self-inflicted by following the rule to write the command into the row: (1) a `FRICTION_EVIDENCE_BEGIN` sentinel check returned `2` — the sentinel plus the row quoting it; (2) an `approval-recorded:` provenance check returned `2` for the same reason after a repair; (3) a separate row's check returned `0` because an unescaped `.` was written where the target carries a backtick. Column-0 anchoring escapes (1) and (2) ONLY because a markdown table cell begins with `|`, which is incidental to table rendering rather than a property the protocol relies on — a ledger rendered as a bulleted list instead of a table would have its quotations at or near column 0 and the anchor would not save it. The durable fix is not more warnings: either mandate that a ledger row's verify command be `grep -F` over a needle the row does NOT reproduce verbatim (e.g. a SHA, a line count, a heading the row names indirectly), or have the ledger store commands in a fenced block the protocol's own instruments mask, so the row's copy is invisible to any scan by construction. The recurrence rate matters more than any single instance: three in one session, each caught only because the expected value was stated alongside the command, which is the one habit that made them visible. — remedy-paths: library/prompts/instructions/protocols/sidequest protocol.md; engaged-set: same as the row above
- [friction-followup] **Diff-QC's gate replay re-runs the delegate's own command, so it cannot detect a gate that passes VACUOUSLY — the one defect class a delegated build is most likely to introduce.** `delegated implementation contract.md` § Main-agent diff-QC step 3 reads "Gate replay. Re-run the pytest and audit deltas the delegate reported. The snapshot files are independent ground truth; the delegate is lower-tier and its green is trusted only after verification." Measured 2026-08-17 on a Phase-3 build: the delegate reported `npm run ci` exit 0, the main agent re-ran `npm run ci` and reproduced exit 0, and the script was nonetheless defective — `runner_status=$?` was taken after a `node --test … | tee "$out"` pipeline, which in POSIX sh captures `tee`'s status, so a FAILING test still exited 0. The replay could not have caught it: a vacuous gate is vacuous on every run, and re-running it reproduces the vacuity rather than exposing it. What caught it was reading the script and constructing an adversarial probe — dropping a deliberately-failing test into the suite and observing exit 0 — which no step of the contract requires and which the agent did on its own initiative. The general shape: step 3 verifies that the delegate's REPORT matches the gate's OUTPUT, and is silent on whether the gate's output carries information. That is the same vacuity argument the corpus already applies to CI test counts and to inert grep rules, unapplied to the verification contract itself. Candidate remedy, not selected: add a step-3 clause requiring that where the delegate BUILT a gate, the main agent demonstrate the gate can FAIL — one adversarial input per newly-authored gate — rather than only that it passes. Note this is the negative-control principle the zotero-email-intake Phase 3 plan already mandates for its own invariant suite ("a gate never observed to fail has not been shown capable of failing"), which is evidence the principle is already accepted in the corpus and merely unapplied at this site. — remedy-paths: library/prompts/instructions/protocols/delegated implementation contract.md; engaged-set: scripts/ci/run-tests.sh, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3 hardening.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/zotero-email-intake-plugin master.md
Routed from the `zotero-specialist --purpose implementation-friction-research` scratch (`2026-08-17_1300_zotero-specialist_implementation-friction-research.md`, topic `phase3-preflight-plan-gaps`) at Step 6 item 4. The `### Research Output` deliverable is NOT here — it is ported under `### Friction Resolution: phase3-preflight-plan-gaps` in the main scratch and sits behind a confirm-then-apply gate.
- **`check_citation_integrity` classifies a `.mjs` citation target as `INVALID_TARGET`, and this is now independently reproduced by a second agent.** The specialist hit it on footnote 12 of the knowledge doc it wrote this round and had to repoint the footnote off `renameFiles.mjs` onto the `attachments.js` import line that consumes the constant, carrying the `.mjs` path in the locator instead. ESM is the Zotero 8+ standard — the Zotero 8 developer docs record that all JSMs became `.mjs`/`.sys.mjs` — so every future Zotero knowledge doc citing modern platform source hits this. The recognized-code-file extension list should gain `.mjs` and plausibly `.cjs`, `.mts`, `.cts`. This CORROBORATES the earlier capture in this session's follow-ups (which named `CODE_CITATION_EXTENSIONS` in `scripts/utils/citation_format.py` and paired it with the `_PATHY_SUFFIX` gap in `check_plan_anchor_freshness`); the two were surfaced by different specialists in different rounds, which is evidence the shared-extension-registry fix is the durable one rather than two independent patches. — remedy-paths: scripts/utils/citation_format.py, scripts/validate/check_plan_anchor_freshness.py; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3 hardening.md, bootstrap.js, src/intake.js, src/message.js, prefs.js, prefs.xhtml, locale/en-US/emailintake.ftl, package.json, scripts/ci/invariants.sh, scripts/ci/run-tests.sh, .github/workflows/ci.yml, test/redos.test.js, docs/architecture.md
- **The donor Zotero plugin loader ships a `getPrefDefault` helper that throws `TypeError` on any non-string preference and silently overwrites a stored `0`, and the defect is UPSTREAM of this plugin.** `getPrefDefault(pref, defaultValue)` evaluates `Zotero.Prefs.get(...)?.trim()`; `?.` guards only null and undefined, so a boolean or integer value reaches `.trim()` and throws, and its falsy check overwrites a deliberate integer `0` with the default on every read. This plugin's remedy is simply not to use it (Zotero's own `setDefaultPrefs` supplies defaults from a plugin-root `prefs.js`), but the same loader underpins `zotero-attachment-scanner` and is widely copied, so the next plugin that copies it inherits the defect. A durable-substrate note or an upstream issue would help. Orthogonal to this release: it concerns other people's plugins, not what Phase 3 ships. — remedy-paths: library/knowledge/zotero/ (a substrate note, not yet created); engaged-set: same as the row above
- **`scripts.utils.git_stage` has two argument-shape traps that cost a specialist two round trips.** It rejects `-m` (the flag is `--message`), and `--stage X --commit --message Y` fails with "`--commit` requires `--paths` and `--message`" — so `--stage` and `--commit` are not composable in one call and the working form is `--commit --paths ... --message ...`. Separately the shared commit-message detector rejected the scope `knowledge` as absent from the `workflow_area` vocabulary; the specialist worked around it with `knowledge-substrate`. Neither is a defect in this dispatch's outcome, but git_stage is the sole sanctioned index mutator and is invoked from prose across the corpus, so a usage example in the specialist commitments atomic — or a directed error naming the working form — would remove a recurring first-attempt failure. This is the same class as the previously-captured `git_stage.py --commit accepts paths only via --paths` item and should be batched with it. — remedy-paths: scripts/utils/git_stage.py, library/prompts/templates/atomic/specialist/specialist commitments.md; engaged-set: same as the rows above
- **The Bash worktree-isolation parser refuses `for` loops, multi-target grep loops, and `PIPESTATUS` pipelines, and it refuses them inside SUBAGENTS too.** The specialist independently hit the same three refusal shapes the main agent hit this session ("this command is too complex to verify that it stays inside the worktree"), and worked around each by splitting into single plain commands run from inside the worktree with no shell redirection. Recording it because the cost is now measured on both sides of a dispatch rather than anecdotally on one: every agent in a worktree PWI pays it, and the corpus prescribes loop-shaped invocations in several places without noting that they are unrunnable there. A one-line note at the prescribing sites, or a documented split-form idiom, would remove the rediscovery. — remedy-paths: library/prompts/instructions/norms/scratch doc writing norms.md (§ Bash from a worktree); engaged-set: same as the rows above
- [friction-followup] **Phase docs carry six priority fields that no actuator maintains and no validator checks, and the two available fixes point in opposite directions.** Measured 2026-08-18. `scripts/generate/generate_planning_tables.py:1606` defines `recompute_priority`, which computes the expected score from the stored dimensions, prints `Stale priority in {rel}`, regex-replaces both the `priority:` and `priority-label:` lines and atomically rewrites the file — so the actuator EXISTS and works. It never reaches a phase doc: `:256` skips `planning_doc_type: phase` whenever the doc sits in a multi-phase directory. The audit states the policy verbatim at `scripts/audit/checks/_structural.py`: "Phase docs inherit priority from their master sibling… Requiring priority fields on each phase would institutionalize duplication and drift risk", backed by exempting `PRIORITY_FIELDS = {impact, urgency, loe, risk, priority, priority-label}` from required-field checks and gating dimension-range validation on `pdt != "phase"`. **The gap is that the exemption stops at VALIDATION and not at AUTHORSHIP**: phase docs are still authored carrying all six fields, so every one of them holds an unmaintained, unvalidated, never-recomputed copy of a number the master owns. Concretely in this plan family: `3 hardening.md` carries `priority: 1.88`, which happens to be consistent with its own dimensions (`calculate(3,2,3,1)` returns exactly 1.88), while `3b duplicate detection and resolution.md` was edited this session to `loe: High` and its `priority: 1.95` — computed from the prior `loe: Medium` — did not move, because nothing recomputes it. The inconsistency is invisible to every gate in the corpus. **Two resolutions and they are opposites, which is why this is a decision rather than a fix:** (1) EXTEND the actuator to recompute phase docs too, which makes the fields real but reinstates exactly the duplication-and-drift the exemption was written to avoid, and requires deciding what a phase's priority even means when the master owns the family's; or (2) STRIP the six fields from phase docs at authoring time, in `promote_plan.py` and the phase-doc template, which honours the stated policy and removes the drift surface entirely but is a corpus-wide frontmatter migration touching every existing phase doc. Option 2 is the one consistent with the policy as written; option 1 is the one consistent with what the docs currently contain. Discovered because a specialist flagged a computed field as stale after a hand edit, then disproved the main agent's diagnosis of WHY — the main agent had concluded "the generator reads rather than derives", which is the exact inverse of the truth and would have sent a fix at the wrong component. — remedy-paths: scripts/generate/generate_planning_tables.py, scripts/audit/checks/_structural.py, scripts/generate/promote_plan.py, library/prompts/templates/atomic/body plan implementation phases.md; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/zotero-email-intake-plugin master.md, README.md, docs/architecture.md, prefs.js, prefs.xhtml, locale/en-US/emailintake.ftl

- [friction-followup] **`insert_into_scratch` has no `--content` flag, and argparse's unambiguous-prefix matching silently binds `--content` to `--content-file` — so a path-shaped argument is read as a FILE and its entire contents are inserted.** Measured 2026-08-18: `insert_into_scratch --scratch {brief} --anchor '#### Plan Name' --position before --content '{absolute path to a phase doc}'` reported `inserted 30036 bytes` and pasted all 182 lines of that phase doc into a composed specialist brief. The call exits 0 and its success message names a byte count, so nothing distinguishes it from a correct insertion except noticing that the count is two orders of magnitude too large. The failure is silent in the direction that matters: the brief remained structurally valid afterward — one column-0 `FRICTION_VMS_BEGIN`, one `FRICTION_VMS_END`, `check_subagent_targets` exit 0, `check_specialist_read_diffs` exit 0 — so every downstream gate would have passed on a brief carrying 182 lines of foreign content. **This is the same class as the already-captured `--scratch` vs `--scratch-doc` divergence and should be batched with it**: `append_scratch_section` DOES define `--content`, `insert_into_scratch` does not, and the corpus prescribes both actuators in adjacent paragraphs — `scratch doc writing norms.md` § Edit-vs-Bash names them in one sentence, and `handle friction SKILL.md` prescribes `append_scratch_section` for the orthogonal-followup disposition and `insert_into_scratch` for newest-first insertion. An agent that has just written one invocation carries the wrong flag into the next, and here the wrong flag does not error — it silently does something else. Two candidate fixes, and unlike the `--scratch-doc` case an alias is the WRONG one: aliasing `--content` to `--content-file` would make the observed behaviour correct-by-definition. Either (a) add a real `--content` argument taking literal text, matching the sibling actuator, or (b) call `parser.parse_args` on a parser constructed with `allow_abbrev=False`, which turns the silent misbinding into an `unrecognized arguments: --content` error. Option (b) is one line and fails closed; option (a) removes the divergence at its root and is the durable fix. Doing (b) alone still leaves an agent one failed invocation per site; doing (a) alone leaves every OTHER prefix of `--content-file` silently bound. — remedy-paths: scripts/utils/insert_into_scratch.py, scripts/generate/append_scratch_section.py, library/prompts/instructions/norms/scratch doc writing norms.md; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md (this round's sole write target), library/_scratch/specialist_calls/2026-08-18_1423_zotero-specialist_implementation-friction-research.md, src/intake.js, src/message.js, scripts/make-msg-fixture.js, test/parse.test.js
- [friction-followup] **No fixture in the corpus exercises `src/cfb.js`'s `readMini` path, and Phase 3b's headerless-`.msg` fixture deliberately does not close that — it routes around it.** Established during Phase 3b's plan-gap round and recorded because the decision is sound but the residual is real. `readStream` at `src/cfb.js:284` routes on `entry.size < cfb.miniCutoff`: short streams take `readMini`, long ones take `readChain`. Real `.msg` files store `PR_SUBJECT`, `PR_SENDER_NAME` and `PR_SENDER_EMAIL_ADDRESS` — a few dozen UTF-16LE bytes each — in the mini-stream, so **production takes `readMini` for exactly the properties the `.msg` fallback map reads**. `scripts/make-msg-fixture.js` builds no mini-FAT and no mini-stream (it writes `ENDOFCHAIN` to header offset 60 and gives Root Entry `start = ENDOFCHAIN, size = 0`), so a short stream in a generated fixture reads back as null. Phase 3b's fixture therefore pads each of the three streams to exactly 4096 bytes to force them through `readChain`, which is the measure the existing `0001-plain-ascii.msg` already takes for its 007D stream.
  **The consequence, stated as the gap rather than as a complaint about the choice**: the fallback map's only automated coverage runs against a container whose internal layout differs from every real one it will meet in production, on precisely the routing decision that distinguishes them. `readMini` is reached by no fixture and no test; a regression in it would be caught only by a developer dropping a real `.msg` in a running Zotero. Phase 3b's DoD row 10 is that drop, and it is a human observation that never re-runs.
  **Why it was declined for Phase 3b, so a later reader does not reopen a settled question**: building a real mini-FAT plus mini-stream plus the Root Entry chain is more generator code than the rest of that phase's fixture work combined, on a phase its own Phase Goal records as roughly double the scope it was authored against; and it buys coverage of a reader path with no recorded defect. Lowering the fixture's declared miniCutoff was considered and rejected on stronger grounds — CFB v3 fixes that field at 4096, `openContainer` reads it without validating it, and a container only this reader accepts cannot adjudicate a disagreement between readers, which is most of what a fixture is for.
  **The work**: extend `scripts/make-msg-fixture.js` to build a real mini-FAT and mini-stream, and emit a third fixture whose three property streams are genuinely short, so `readMini` is exercised at parser level. Post-v1; it is a test-coverage gap, not a broken shipped artifact — `readMini` itself is Phase 2 code that production exercises. Note this shares a file with Phase 3b's `affected_files`, so it is orthogonal by SCOPE rather than by surface: the phase declines the work explicitly and names the limit in its own `scripts/make-msg-fixture.js` entry, and this capture exists so the residual survives that entry moving to `implemented/`. — remedy-paths: scripts/make-msg-fixture.js, test/parse.test.js, src/cfb.js; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md, src/intake.js, src/message.js, prefs.js, locale/en-US/emailintake.ftl, scripts/make-msg-fixture.js, test/parse.test.js, docs/architecture.md
- [friction-followup] **A knowledge doc written DURING a cross-repo PWI cannot pass `check_citation_integrity` on any citation to the project's own source, and both available citation forms fail for different reasons.** Measured 2026-08-18 on `library/knowledge/zotero/msg container property fallback and cfb stream routing.md`, written by `zotero-specialist` during Phase 3b research and committed at `8019d4ce75`. Seven footnotes cite `src/message.js`, `src/cfb.js` and `scripts/make-msg-fixture.js`.
  **Form 1 — bare relative traversal — fails on DEPTH.** `citation format reference.md` states this form "works today", and it does from a MAIN tree: `../../../../zotero-email-intake/src/message.js` from `{AW}/library/knowledge/zotero/` resolves correctly. From a WORKTREE it does not, because the worktree inserts `.claude/worktrees/{slug}/` and the same four levels land at `{AW}/.claude/worktrees/zotero-email-intake/...`. The doc was written in the worktree because the dispatch instruction routes knowledge writes there (the guard-exempt path), so the two conventions are in direct conflict: the sanctioned WRITE location makes the sanctioned CITATION form wrong.
  **Form 2 — the preferred `{workspace-key}:` form — fails on TREE.** Rewriting all seven to `zotero-email-intake:src/message.js` changed the error rather than clearing it: `FILE_NOT_FOUND — Cross-repo file not found in cloned repo 'zotero-email-intake'`. The key resolves against `managed_workspaces.yaml` `repo_path`, which is the project MAIN tree — and during a PWI that tree holds only the pre-branch scaffold. Verified: `ls /home/dcl3nd/dev/zotero-email-intake/` returns `AGENTS.md, bootstrap.js, CLAUDE.md, docs, manifest.json, README.md` and no `src/` at all. Every file Phases 0-3 created lives only on the branch until closeout.
  **So the failure is structural, not an authoring error, and it is invisible in the output.** `FILE_NOT_FOUND` reads identically for a typo, a wrong path, and a path that is correct-but-not-yet-merged. An agent that trusts the checker concludes its citations are wrong and rewrites correct ones; an agent that dismisses it learns to discount the gate. Neither is available as a *correct* reading from the message alone.
  **The rewrite to Form 2 was retained anyway and that is a deliberate call, not a fix that failed.** Form 2 is layout-independent and resolves the moment the closeout merge lands `src/` on the project trunk; Form 1 would remain wrong in the worktree and wrong-by-depth forever. The doc is therefore green-after-merge rather than green-now.
  **This is the same structural class as the `check_plan_anchor_freshness` finding routed beside it** — a validator resolving targets against a MAIN tree while a PWI holds them on a branch — reached through a second, independent door. Two validators, two target classes, one cause. That correspondence is the argument for a shared resolution (a worktree-aware target root the whole validator family consumes) rather than two independent patches. The plan-anchor case already has a flag (`--repo-root`); `check_citation_integrity` exposes no equivalent, which is the asymmetry worth closing.
  **One candidate the corpus already contains and which should be checked before designing anything**: `citation format reference.md` names `SIBLING_REPO_ABSENT` as the ONE soft, never-blocking failure for cross-repo targets, on the reasoning that an uncloned sibling is a machine fact rather than an authoring error. A file present in the repo but only on a branch is the same category of fact — true of this machine at this moment, not of the citation — which suggests the soft class is under-drawn rather than that a new mechanism is needed. — remedy-paths: scripts/validate/check_citation_integrity.py, library/prompts/instructions/protocols/citation format reference.md; engaged-set: library/knowledge/zotero/msg container property fallback and cfb stream routing.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md, the in-flight sidecar
- [friction-followup] **CORRECTION to the "Missing: the Zotero client source tree" substrate gap routed above — the gap is FALSIFIED, and the way it was falsified is the durable finding.** The specialist logged that gap twice: in round 4 as the reason it would not recommend the submenu option for the resolution dialogs, and in round 6 as the reason it could not verify `MenuManager.unregisterMenu` and therefore left plugin-menu teardown to the platform. Both claims rested on "the Zotero source is not present in either worktree and the JavaScript API page does not document `MenuManager` at all."
  **The source clone is indeed absent, and it did not matter.** `library/knowledge/zotero/codebases/zotero architecture.md` — 57 KB, inside the specialist's OWN substrate directory, listed in its own substrate TOC — carries every fact both claims needed:
  - Line 122 and line 183 document `Zotero.MenuManager.registerMenu({target, menuID, pluginID, menus})`, enumerate `VALID_TARGETS`, and state the menu types verbatim as `menuitem` / `separator` / `submenu`. **Submenu support is documented**, so round 4's first ground against the submenu option was false. (The recommendation it reached may still be right — its other two grounds, the DoD-count contradiction and a menu having nowhere to carry an explanation, are untouched by this. Only one of three grounds fell.)
  - Line 180 states that `PluginAPIBase` subclasses "install a shutdown observer that unregisters all options for a removed plugin", and line 343 names the mechanism: "On plugin shutdown the shutdown observer in `Zotero.Plugins` calls `_unregisterByPluginID(pluginID)` which removes every registration owned by that plugin." **This CONFIRMS round 6's teardown choice** — the platform auto-unregisters, so not calling `unregisterMenu` is correct rather than merely precedented.
  - Line 436 adds the related fact neither round reached: `_namespacedMainKey()` CSS-escapes and namespaces every registered key, so the id returned from `register*()` may differ from the one passed in, and plugins must store the RETURNED key for any later `unregister*()` call. The implementation stores returned ids in `EmailIntake._menuIDs`, which is consistent with this.
  **So the failure was not a missing artifact but an unconsulted one**, and that is a different defect with a different remedy. A clone would have closed it; so would reading a doc already in substrate, at a fraction of the cost. The specialist reported adequate Zotero-side substrate in the same breath as this gap — "the Zotero-side substrate carried the parts of this question that touch the host" — so it believed it had checked. The candidate remedy is therefore NOT acquisition: it is that a substrate-gap claim about a cloned-repo surface should be required to name the architecture doc it consulted first, the way a blocker claim is required to name its deciding command. `blocker claims require artifacts.md` already governs the general shape; what is absent is its application to the `### Substrate Gaps` section, where a gap is asserted from a null observation ("not present in either worktree") without the cheaper positive check being run.
  **Cost of the instance, stated so the remedy is sized honestly**: one design option declined on a false ground, one shipped behaviour left unverified through implementation, one user decision taken on an acquisition that was never needed, and a follow-up routed for a clone the corpus did not require. Nothing shipped wrong — the teardown choice was right — but it was right by precedent rather than by the verification available. — remedy-paths: library/prompts/templates/atomic/specialist/specialist substrate gaps section.md, library/prompts/instructions/norms/blocker claims require artifacts.md; engaged-set: src/intake.js, src/message.js, scripts/make-msg-fixture.js, test/parse.test.js, locale/en-US/emailintake.ftl, docs/architecture.md, library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md
- [friction-followup] **The Phase 3b manual-test kit is untracked, unversioned, and has no generator, so every correction to it dies with the kit.** Measured 2026-08-20: `grep -rn 'phase3b-manual-tests'` across the project worktree (`--include=*.js --include=*.sh --include=*.json --include=*.md`) returns ZERO hits. The kit at `~/Desktop/zotero-email-intake/phase3b-manual-tests/` — a built `.xpi`, a `drops/` tree including a fifty-copy directory, and a 177-line hand-authored README carrying the ten-test walkthrough — exists only on the Desktop. It is not committed, not gitignored-with-a-source, and not produced by any script in the repo. **Two defects found in that README this run are the evidence this matters**: its Test 6 snippet uses top-level `return`, which Zotero's Run JavaScript rejects with `SyntaxError: return not in function` unless the Async box is ticked; and neither it nor the plan warns that `EmailIntake._searchCount` is zeroed by a plugin reload, while the README's own prescribed test ORDER puts a deliberate disable/re-enable (Test 8e) between Test 6 and any later reading — so the suite destroys its own evidence. Both were diagnosed and both cost a full manual re-run. Correcting the Desktop copy fixes this kit and nothing else: Phase 4 will build another kit, authored from the plan, and both traps recur unless the plan carries them. **This round's fix routes the two traps into the plan's Validation item 5 for exactly that reason — this follow-up is the ORTHOGONAL half, which is the kit's own lifecycle.** Three candidate remedies, none selected: (a) commit the kit under `test/manual/` so the README is versioned and reviewable, at the cost of committing a built `.xpi` and a fifty-copy fixture tree; (b) generate the kit from a script in the repo, so the README is authored once and the drops are reproducible, which is the durable answer and the most work; (c) keep the kit ephemeral by design and make the PLAN the sole authority for test procedure, so the README shrinks to a pointer — cheapest, and it concedes that a hand-authored walkthrough will drift from the plan it restates. Note that (c) is closest to what this round's fix already does for two items, so the decision is partly whether to finish that direction or reverse it. — remedy-paths: test/manual/ (does not yet exist), scripts/ (a kit generator, does not yet exist), library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md, src/intake.js
Routed from the `zotero-specialist --purpose implementation-friction-research` scratch (`2026-08-18_1423_zotero-specialist_implementation-friction-research.md`, topic `phase3b-underspecified-affected-files`) at Step 6 item 4. **These nine were owed routing across five specialist rounds and were not routed at the time** — the main agent ran the Friction Resolution post-completion block and skipped item 4's base-report routing, which that block does not waive. Every item is appended without pre-filtering, including two that duplicate captures made earlier this session, because pre-filtering at routing time is prohibited and the triage surface owns that call.
**From `### Follow-up Ideas` (6 items):**
- [halt-record, RESOLVED] Phase 3b implementation halted before first write on four items meeting the §8.2 sensitive-halt bar: `debugTrace`/`onParseFailure` committed in the Phase Goal and specified in no File-by-File entry; `_searchCount` required by Validation item 5 and a DoD row and specified in no entry; tag names contradicted between the Phase Goal's Fluent-only rule and the literal `#duplicate-message`/`#auto-attached-duplicate` in the outcomes list; and the resolution-dialog mechanism undecided while being upstream of the Fluent strings it determines. All four were workshopped to full concurrence and closed by VMS 9 through VMS 15, applied and committed at `a2aed682b9`. Recorded because the halt itself is the evidence that a specialist refusing to implement caught four commitments no gate did.
- **`check_plan_anchor_freshness` reports a branch-only plan doc as six spec defects when run from a worktree PWI without `--repo-root`, and the instruction that prescribes the self-check never names that flag.** The specialist hit this and concluded no worktree fallback exists, proposing a new `--tree-root` flag. **That conclusion is FALSIFIED and the correction is the finding**: `--repo-root` already exists (`--help` lists it), and passing it returned `15/15 target(s) resolved` on the identical deliverable that returned `target-not-found` on every spec without it. The defect is discoverability rather than capability, and it is worse than a missing feature: the bare form returns a clean-looking failure that a careful agent reasons its way into trusting, and the atomic that prescribes the self-check (`body specialist research output.md` counter-prescription 5) prescribes exactly that bare form. Every VMS a specialist authors against its own calling plan during a worktree PWI hits it. — remedy-paths: library/prompts/templates/atomic/specialist/body specialist research output.md; engaged-set: library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/3b duplicate detection and resolution.md, library/knowledge/zotero/zotero notifier sequencing and drag-drop promotion mechanics.md, the in-flight sidecar
- **The `library/knowledge/zotero/` footnote corpus predates the current Pattern-A checker and fails it wholesale.** `zotero item dedup lookup surfaces.md` — and, from a spot read, its siblings — use `` `~/dev/path` line N: "quote" ``, which `check_citation_integrity` now rejects as `FORMAT_VIOLATION` ("a backticked path is not a link"). Writing a new doc in the directory means writing in a form none of its neighbours use. Worth a single pass converting the directory to Pattern-A, or an explicit decision to grandfather it; either beats the current state, where the convention a reader infers from the neighbours is the one the checker refuses. — remedy-paths: library/knowledge/zotero/; engaged-set: same as the row above
- **`zotero item dedup lookup surfaces.md` carries `last_verified` but neither `last_edited` nor `last_edit_description`.** Per `## Knowledge File Maintenance` these are the current required fields. The specialist read 30 of its 185 lines (schema reference only) and did not repair it, because repairing the footnote format alongside requires reading every footnote and re-opening its cited source. Bundle with the directory-wide conversion above. — remedy-paths: library/knowledge/zotero/zotero item dedup lookup surfaces.md; engaged-set: same as the rows above
- [friction] The Bash worktree-isolation parser refused two compound commands — a `for` loop over grep targets, and a `cd &&` plus `${PIPESTATUS[0]}` — neither of which touched git. Worked around by splitting into single-purpose calls and using `conda run --cwd` instead of `cd &&`. Duplicates an earlier capture this session; routed anyway per the no-pre-filtering rule. — remedy-paths: library/prompts/instructions/norms/scratch doc writing norms.md; engaged-set: same as the rows above
- [friction] `git_stage --commit` rejected a knowledge-doc commit message because the scope `knowledge` is not a `workflow_area` member; worked around with `knowledge-substrate`, one of the five the rejection enumerated. Duplicates an earlier capture this session; routed anyway. — remedy-paths: scripts/utils/git_stage.py; engaged-set: same as the rows above
**From `### Substrate Gaps` (3 gaps, routed unconditionally per Step 6 item 4; the `/manage-substrate` offer is a separate synchronous path):**
- Missing: `[MS-CFB]` Compound File Binary File Format specification — Type: `web-cache` — Reference: https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-cfb/53989ce4-7b05-4f8d-829b-d08d6148375b. **This gap is LOAD-BEARING ON AN ALREADY-APPLIED SPEC and is the sharpest of the three.** Everything the specialist established about mini-stream routing, the 4096 cutoff and the red-black directory tree came from reading `src/cfb.js`'s implementation and comments — the strongest available grounding for what THIS reader does and no grounding at all for what the FORMAT requires. VMS 1's rejection of the lower-the-miniCutoff fixture option rests on 4096 being spec-fixed for CFB v3, which is held from the reader's comments rather than the specification. Worth caching before any further fixture-generator work.
- Missing: `[MS-OXMSG]` Outlook Item (.msg) File Format specification — Type: `web-cache` — Reference: https://learn.microsoft.com/en-us/openspecs/exchange_server_protocols/ms-oxmsg/b046868c-9fbf-41ae-9ffb-8de2bd4eec82. The phase doc itself schedules acquiring this post-v1 and names it as the precondition for parsing PT_SYSTIME date properties out of `__properties_version1.0`. It would also confirm the three fallback property tags and their `001F` PT_UNICODE stream naming independently of this codebase's own comments.
- Missing: the Zotero client source tree, specifically `Zotero.MenuManager`'s `MenuData` typedef — Type: `clone` — Reference: https://github.com/zotero/zotero. Surfaced in round 4: option D of the resolution-dialog decision (a submenu rather than a modal) turns on whether `MenuManager` accepts `menuType: 'menu'` with a nested `menus` array. The specialist believed it does and could not verify it — the Zotero source is in neither worktree and the JavaScript API page does not document `MenuManager` at all. **That unverifiability is load-bearing rather than incidental**: it is one of three reasons D was not recommended, so a clone could change the recommendation rather than merely tidy a citation.
- [friction-followup] **`complete_plan.py`'s master path is NON-RESUMABLE after a claim-release push failure, because it writes the field it later guards re-entry on.** **Measured 2026-08-21** on this plan's own completion. The sequence in `handle_master` is: `_set_frontmatter_completed` → `_release_plan_claim` (which commits the registry removal on `MAIN_TREE_ROOT` and then pushes) → `_git_mv`. The push failed with `! [rejected] main -> main (non-fast-forward)` because the AW main tree was 81 ahead / 1 behind `origin/main` — a pre-existing condition of that tree, not something this session created. `_release_plan_claim` raises `PlanError` only AFTER its commit, so the run aborted with `plan_status: completed` already written and the directory NOT moved.
  **Why that half-state is worse than it looks, and it is not cosmetic.** `library/knowledge/agentic-workspace/the completed directory is the active-plan partition key not plan_status.md` establishes at :29-36 that every consumer answering "is this plan active?" reads the PATH SEGMENT — `generate_planning_tables.py`, `check_plan_overlap.py`, `check_plan_conflicts.py`, and the audit's planning checks — and that "no consumer uses `plan_status` to decide active-vs-landed." So the aborted state left a plan that read ACTIVE to every consumer while its frontmatter said completed. A concurrent plan could have co-mutated its `affected_files` undetected for as long as the state persisted.
  **And the actuator locks itself out of the repair.** The same doc records at :36 that `complete_plan.py` "refuses to complete an already-`completed` plan", which is precisely the field the aborted run had just written. Re-invoking the sanctioned actuator returns `ERROR: plan_status is already completed` — so the one tool that owns the lifecycle move cannot perform it.
  **The defect class is already documented and the recovery is not.** `library/knowledge/software-engineering/fail-closed by position in a rollback-less incremental actuator.md` describes this actuator precisely, including at :59 that `_release_plan_claim` "rais[es] `PlanError` only AFTER the commit if the push fails — i.e., the commit is already made." That doc's subject is where to POSITION a new gate; it prescribes nothing for the state a failed push leaves behind, and no norm, protocol, or skill body found in `library/prompts/instructions/`, `library/docs/stipulations/` or `library/knowledge/` covers recovery from it.
  **The recovery that worked, recorded so the next reader does not re-derive it.** Restore `plan_status: in_progress` on the uncommitted frontmatter line and re-run. `_release_plan_claim` is idempotent by construction — `complete_plan.py:251` returns early with the comment "no claim entry (already released, or never claimed) — idempotent no-op" once `plan_claim_registry.release_claim` finds the file already unlinked — so the second run never reaches the push that aborted the first, and it completes the move. Verified: second run exited 0 with `Done.`, the family moved to `features/completed/`, and the planning tables re-rendered at 0 active plans for the workspace.
  **Candidate remedies, none selected.** (1) Reorder `handle_master` so `_git_mv` precedes `_release_plan_claim` — the move is local and reversible-in-principle where the push is neither, and it would leave the partition key correct even when the network step fails; the cost is that a sibling clone briefly sees a landed plan whose claim is still held, which is the safer of the two inconsistencies. (2) Make the already-completed precondition tolerant of a plan whose `plan_status` is completed but whose path is NOT under `completed/` — that state is unambiguously a partial completion and is exactly the one the guard should admit rather than refuse. (3) Push-check before mutating: a pre-mutation `git -C {main} push --dry-run` fail-closed precondition, which is the shape `fail-closed by position in a rollback-less incremental actuator.md` § How to apply already prescribes for this actuator ("position the gate as a pure read-check before the FIRST side effect"). Option 3 is the one that doc's own guidance points at; option 2 is the smallest and repairs the lockout specifically.
  **Residual left by this instance, and it is NOT resolved by the recovery above.** The claim-release commit `b139cc74cb` sits on the AW MAIN tree unpushed, and main has since advanced to `258b3ce847`. Sibling sessions reading `origin/main` therefore still see the claim held for `library/docs/planning/projects/zotero-email-intake/features/zotero-email-intake-plugin/zotero-email-intake-plugin master.md`, on a plan that is now completed and moved. Clearing it requires reconciling that tree with `origin/main` and pushing — main-cwd work, per `library/knowledge/git/worktree-cwd-to-main-write-refusal-and-readonly-main-landing-infeasibility.md`, and unreachable from a worktree-isolated session. — remedy-paths: scripts/generate/complete_plan.py, library/knowledge/software-engineering/fail-closed by position in a rollback-less incremental actuator.md; engaged-set: library/docs/planning/projects/zotero-email-intake/features/completed/zotero-email-intake-plugin/zotero-email-intake-plugin master.md, scripts/utils/plan_claim_registry.py

## Worktree Status

- branch: worktree-08-15_1300_zotero-email-intake-plugin-pwi
- path: /home/dcl3nd/dev/zotero-email-intake/.claude/worktrees/08-15_1300_zotero-email-intake-plugin-pwi

